# pavois.dev — PRE-LAUNCH: indexing disabled site-wide.
# Every page also emits <meta name="robots" content="noindex, nofollow"> (see src/config/site.ts).
# At launch: set INDEXABLE = true in src/config/site.ts AND restore the allow policy below.
User-agent: *
Disallow: /

# --- launch policy (commented out until ready) ---
# User-agent: *
# Allow: /
# Sitemap: https://pavois.dev/sitemap.xml
