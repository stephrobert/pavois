{
  "id": "pkg-abrt-plugin-sosreport-removed",
  "supported_os": [
    "rhel8",
    "rhel9"
  ],
  "os_versions": {
    "rhel8": "4.0.0",
    "rhel9": "2.0.0"
  },
  "severity": "low",
  "domain": "Packages",
  "evidence_type": "inventory-state",
  "reboot_survivable": "yes",
  "norms": {},
  "check": [
    "describe package('abrt-plugin-sosreport') do",
    "  it { should_not be_installed }",
    "end"
  ],
  "remediation": {
    "action": "remove",
    "name": "abrt-plugin-sosreport",
    "resource": "package"
  },
  "title": {
    "en": "Uninstall abrt-plugin-sosreport Package",
    "fr": "Désinstaller le paquet abrt-plugin-sosreport"
  },
  "summary": {
    "en": "Removes the **`abrt-plugin-sosreport`** package so full system diagnostics are not bundled into ABRT crash reports.",
    "fr": "Supprime le paquet **`abrt-plugin-sosreport`** afin que des diagnostics système complets ne soient pas intégrés aux rapports de plantage ABRT."
  },
  "rationale": {
    "en": "`abrt-plugin-sosreport` lets ABRT attach a full **sosreport** to a crash report. An sosreport is a sweeping system diagnostic bundle (configuration, logs, network, package state), embedding it in crash data dramatically widens what can leak if a report is exfiltrated or sent off-host. On a hardened server this plugin is unnecessary and should be removed.",
    "fr": "`abrt-plugin-sosreport` permet à ABRT de joindre un **sosreport** complet à un rapport de plantage. Un sosreport est un ensemble de diagnostics système très large (configuration, journaux, réseau, état des paquets), l'intégrer aux données de plantage élargit considérablement ce qui peut fuir si un rapport est exfiltré ou envoyé hors de l'hôte. Sur un serveur durci, ce plugin est superflu et doit être supprimé."
  },
  "check_note": {
    "en": "Pavois queries the **effective** package database via the `package('abrt-plugin-sosreport')` resource (`rpm -q`), reporting what is actually installed now rather than any config or manifest, the live RPM database is authoritative.",
    "fr": "Pavois interroge la base de paquets **effective** via la ressource `package('abrt-plugin-sosreport')` (`rpm -q`), indiquant ce qui est réellement installé à l'instant T plutôt qu'une configuration ou un manifeste, la base RPM réelle fait foi."
  },
  "verify": {
    "en": "Run `rpm -q abrt-plugin-sosreport`. Expected output: `package abrt-plugin-sosreport is not installed`. A version string means the rule fails.",
    "fr": "Exécutez `rpm -q abrt-plugin-sosreport`. Sortie attendue : `package abrt-plugin-sosreport is not installed`. Une chaîne de version signifie que la règle échoue."
  },
  "logs": {
    "en": "Confirm with `rpm -q abrt-plugin-sosreport`; the removal is logged in `dnf history` and `/var/log/dnf.rpm.log`.",
    "fr": "Confirmez avec `rpm -q abrt-plugin-sosreport` ; la suppression est journalisée dans `dnf history` et `/var/log/dnf.rpm.log`."
  },
  "remediation_note": {
    "en": "Pavois's harden plan uses the **`package`** resource with action **`remove`** to uninstall `abrt-plugin-sosreport` (equivalent to `dnf remove abrt-plugin-sosreport`). Apply it with **`pavois harden apply`**.",
    "fr": "Le plan de durcissement de Pavois utilise la ressource **`package`** avec l'action **`remove`** pour désinstaller `abrt-plugin-sosreport` (équivalent à `dnf remove abrt-plugin-sosreport`). Appliquez-le avec **`pavois harden apply`**."
  },
  "impact": {
    "en": "Removing this plugin only stops ABRT from auto-attaching sosreports; the standalone `sos` tool (if installed) still works for on-demand diagnostics. **Precautions:** none significant, no service interruption, and reinstalling the package restores the behaviour. Keep the standalone `sos` package if support cases require sosreports.",
    "fr": "Supprimer ce plugin empêche seulement ABRT de joindre automatiquement des sosreports ; l'outil autonome `sos` (s'il est installé) reste utilisable pour des diagnostics à la demande. **Précautions :** aucune notable, pas d'interruption de service, et réinstaller le paquet rétablit le comportement. Conservez le paquet `sos` autonome si vos dossiers de support nécessitent des sosreports."
  },
  "datePublished": "2026-06-28",
  "dateModified": "2026-07-14"
}
