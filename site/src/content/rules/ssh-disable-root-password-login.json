{
  "id": "ssh-disable-root-password-login",
  "supported_os": [
    "debian12",
    "debian13",
    "fedora",
    "rhel10",
    "rhel8",
    "rhel9",
    "ubuntu2404",
    "ubuntu2604"
  ],
  "os_versions": {
    "debian12": "1.1.0",
    "debian13": "1.0.0",
    "rhel8": "4.0.0",
    "rhel9": "2.0.0",
    "ubuntu2404": "1.0.0"
  },
  "severity": "medium",
  "domain": "SSH",
  "evidence_type": "effective-runtime",
  "reboot_survivable": "yes",
  "norms": {},
  "check": [
    "describe command('sshd -T') do",
    "  its('stdout') { should match(/^permitrootlogin\\s+(prohibit\\-password|without\\-password|no)$/i) }",
    "end"
  ],
  "remediation": {},
  "title": {
    "en": "Disable SSH root Login with a Password (Insecure)",
    "fr": "Interdire la connexion SSH root par mot de passe (non sécurisé)"
  },
  "summary": {
    "en": "Requires **`PermitRootLogin prohibit-password`** so root may only log in over SSH with a key, never with a password.",
    "fr": "Exige **`PermitRootLogin prohibit-password`** afin que root ne puisse se connecter en SSH qu'avec une clé, jamais avec un mot de passe."
  },
  "rationale": {
    "en": "This is the weaker, compatibility-oriented alternative to fully disabling root SSH: `prohibit-password` still permits direct root login but only via public-key (or other non-interactive) authentication, blocking password-based logins. This removes root's password from the online brute-force surface while keeping key-based automation that legitimately needs root. Prefer `PermitRootLogin no` where operationally possible.",
    "fr": "C'est l'alternative plus faible, orientée compatibilité, à la désactivation complète du SSH root : `prohibit-password` autorise encore la connexion root directe mais uniquement par clé publique (ou autre authentification non interactive), bloquant les connexions par mot de passe. Cela retire le mot de passe de root de la surface de force brute en ligne tout en conservant l'automatisation par clé qui a légitimement besoin de root. Préférez `PermitRootLogin no` lorsque c'est opérationnellement possible."
  },
  "check_note": {
    "en": "Pavois reads the **effective** value from `sshd -T`, after all `Include`s, `/etc/ssh/sshd_config.d/` drop-ins and `Match` blocks. The distinction between `no`, `prohibit-password` and `yes` is only reliable from the resolved output; a read of `/etc/ssh/sshd_config` could be overridden by a later drop-in.",
    "fr": "Pavois lit la valeur **effective** via `sshd -T`, après tous les `Include`, drop-ins de `/etc/ssh/sshd_config.d/` et blocs `Match`. La distinction entre `no`, `prohibit-password` et `yes` n'est fiable que depuis la sortie résolue ; une lecture de `/etc/ssh/sshd_config` pourrait être surchargée par un drop-in ultérieur."
  },
  "verify": {
    "en": "Run `sshd -T | grep -i '^permitrootlogin'`. Expected output is `permitrootlogin prohibit-password`.",
    "fr": "Exécutez `sshd -T | grep -i '^permitrootlogin'`. La sortie attendue est `permitrootlogin prohibit-password`."
  },
  "logs": {
    "en": "Root login attempts are logged in `/var/log/secure` or via `journalctl -u sshd` (RHEL family); a password attempt for root is rejected and shows as a failed authentication line.",
    "fr": "Les tentatives de connexion root sont journalisées dans `/var/log/secure` ou via `journalctl -u sshd` (famille RHEL) ; une tentative par mot de passe pour root est rejetée et apparaît comme une ligne d'échec d'authentification."
  },
  "remediation_note": {
    "en": "This rule has **no automated harden plan**, the `remediation` object is empty. Apply it manually by setting `PermitRootLogin prohibit-password` in a drop-in under `/etc/ssh/sshd_config.d/`, validating with `sshd -t`, then reloading sshd (`systemctl reload sshd`). Consider `PermitRootLogin no` instead for stronger hardening.",
    "fr": "Cette règle n'a **aucun plan harden automatisé**, l'objet `remediation` est vide. Appliquez-la manuellement en posant `PermitRootLogin prohibit-password` dans un drop-in de `/etc/ssh/sshd_config.d/`, en validant avec `sshd -t`, puis en rechargeant sshd (`systemctl reload sshd`). Envisagez plutôt `PermitRootLogin no` pour un durcissement plus fort."
  },
  "impact": {
    "en": "**Before applying**, ensure root has an authorized SSH key installed (`/root/.ssh/authorized_keys`) if any workflow needs root SSH, or that an admin account with `sudo` exists, otherwise you may cut off the only root path. Test a **second SSH session** before closing the current one. Note this still allows direct root login by key, so it is weaker than `PermitRootLogin no`; password-only root automation will break and must move to keys.",
    "fr": "**Avant d'appliquer**, assurez-vous que root dispose d'une clé SSH autorisée (`/root/.ssh/authorized_keys`) si un flux nécessite le SSH root, ou qu'un compte admin avec `sudo` existe, sinon vous risquez de couper le seul accès root. Testez une **deuxième session SSH** avant de fermer la session courante. Notez que cela autorise toujours la connexion root directe par clé, donc c'est plus faible que `PermitRootLogin no` ; l'automatisation root par mot de passe seul cassera et devra passer aux clés."
  },
  "datePublished": "2026-06-28",
  "dateModified": "2026-07-14"
}
