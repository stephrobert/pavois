{
  "id": "fileperm-user-cfg",
  "supported_os": [
    "debian12",
    "debian13",
    "fedora",
    "rhel10",
    "rhel8",
    "rhel9",
    "ubuntu2404",
    "ubuntu2604"
  ],
  "os_versions": {
    "debian12": "1.1.0",
    "debian13": "1.0.0",
    "rhel8": "4.0.0",
    "rhel9": "2.0.0",
    "ubuntu2404": "1.0.0"
  },
  "severity": "medium",
  "domain": "File permissions",
  "evidence_type": "persistent-config",
  "reboot_survivable": "yes",
  "norms": {
    "bp28": "R29",
    "cis": [
      "2.2.6",
      "1.4.2"
    ],
    "nist": [
      "3.4.5",
      "AC-6(1)",
      "CM-6(a)"
    ],
    "pci-dss": "2.2.6"
  },
  "check": [
    "only_if { file('/boot/grub/user.cfg').exist? }",
    "describe file('/boot/grub/user.cfg') do",
    "  it { should_not be_executable.by('owner') }",
    "  it { should_not be_setuid }",
    "  it { should_not be_executable.by('group') }",
    "  it { should_not be_writable.by('group') }",
    "  it { should_not be_readable.by('group') }",
    "  it { should_not be_setgid }",
    "  it { should_not be_executable.by('other') }",
    "  it { should_not be_writable.by('other') }",
    "  it { should_not be_readable.by('other') }",
    "  it { should_not be_sticky }",
    "end"
  ],
  "remediation": {
    "mode": "0600",
    "path": "/boot/grub/user.cfg",
    "resource": "file"
  },
  "title": {
    "en": "Verify /boot/grub2/user.cfg Permissions",
    "fr": "Vérifier les permissions de /boot/grub2/user.cfg"
  },
  "summary": {
    "en": "Ensures `/boot/grub2/user.cfg` (the GRUB superuser password file) is readable and writable only by `root`, with no execute/setuid/setgid/sticky bits.",
    "fr": "Garantit que `/boot/grub2/user.cfg` (le fichier de mot de passe superuser GRUB) n'est lisible et modifiable que par `root`, sans bits d'exécution/setuid/setgid/sticky."
  },
  "rationale": {
    "en": "`/boot/grub2/user.cfg` stores the **GRUB bootloader superuser password hash** (PBKDF2). If group or other can read it, an attacker can crack the hash offline and gain the GRUB superuser password, letting them edit boot entries, drop to a root shell via `init=/bin/bash`, or disable security at boot, a full bypass of OS-level access controls. If the file is writable by a non-root user, they can replace or remove the password protection entirely. Only `root` should read or modify it.",
    "fr": "`/boot/grub2/user.cfg` stocke le **hash du mot de passe superuser du chargeur GRUB** (PBKDF2). Si le groupe ou les autres peuvent le lire, un attaquant peut casser le hash hors ligne et obtenir le mot de passe superuser GRUB, ce qui lui permet d'éditer les entrées de démarrage, d'obtenir un shell root via `init=/bin/bash` ou de désactiver la sécurité au démarrage, un contournement complet des contrôles d'accès du système d'exploitation. Si le fichier est modifiable par un utilisateur non-root, celui-ci peut remplacer ou supprimer entièrement la protection par mot de passe. Seul `root` devrait pouvoir le lire ou le modifier."
  },
  "check_note": {
    "en": "Pavois reads the **effective inode mode** of `/boot/grub2/user.cfg` and asserts no read/write for group or other and no special bits. Checking the live file (rather than assuming the generator wrote it correctly) catches cases where the file was regenerated, copied, or `chmod`-ed after creation and silently became group/world-readable.",
    "fr": "Pavois lit le **mode effectif de l'inode** de `/boot/grub2/user.cfg` et vérifie l'absence de lecture/écriture pour le groupe ou les autres et l'absence de bits spéciaux. Vérifier le fichier réel (plutôt que de supposer que le générateur l'a écrit correctement) détecte les cas où le fichier a été régénéré, copié ou `chmod`-é après création et est devenu silencieusement lisible par le groupe/tous."
  },
  "verify": {
    "en": "Run `stat -c '%U %G %a' /boot/grub2/user.cfg`. Expected: owner/group `root` with mode `600` (or `0600`), readable/writable by root only, no bits for group/other.",
    "fr": "Exécutez `stat -c '%U %G %a' /boot/grub2/user.cfg`. Attendu : propriétaire/groupe `root` avec le mode `600` (ou `0600`), lisible/modifiable par root uniquement, aucun bit pour le groupe/les autres."
  },
  "logs": {
    "en": "There is no runtime log for this file. Integrity drift can be detected with `rpm -Vf /boot/grub2/user.cfg` (RHEL/Alma/Fedora). GRUB regeneration that may rewrite it is triggered by `grub2-mkconfig`; review its output and re-check the mode afterward.",
    "fr": "Il n'existe pas de journal d'exécution pour ce fichier. La dérive d'intégrité peut être détectée avec `rpm -Vf /boot/grub2/user.cfg` (RHEL/Alma/Fedora). La régénération de GRUB qui peut le réécrire est déclenchée par `grub2-mkconfig` ; examinez sa sortie et revérifiez le mode ensuite."
  },
  "remediation_note": {
    "en": "No automated remediation ships for this rule. Tighten the file manually: `chown root:root /boot/grub2/user.cfg && chmod 600 /boot/grub2/user.cfg`.",
    "fr": "Aucune remédiation automatique n'est fournie pour cette règle. Restreignez le fichier manuellement : `chown root:root /boot/grub2/user.cfg && chmod 600 /boot/grub2/user.cfg`."
  },
  "impact": {
    "en": "A readable `user.cfg` leaks the GRUB superuser hash (offline crackable, leading to boot-time root access); a writable one lets an attacker disable boot password protection. Setting it to `0600 root:root` is safe and matches the GRUB default. **Precaution:** do not delete or empty the file, that removes GRUB password protection. Re-run `grub2-mkconfig` only when needed, since regeneration can reset the mode.",
    "fr": "Un `user.cfg` lisible divulgue le hash superuser GRUB (cassable hors ligne, menant à un accès root au démarrage) ; un fichier modifiable permet à un attaquant de désactiver la protection par mot de passe au démarrage. Le passer à `0600 root:root` est sans danger et correspond à la valeur par défaut de GRUB. **Précaution :** ne supprimez pas et ne videz pas le fichier, cela retirerait la protection par mot de passe GRUB. Ne relancez `grub2-mkconfig` que si nécessaire, car la régénération peut réinitialiser le mode."
  },
  "datePublished": "2026-06-28",
  "dateModified": "2026-07-14"
}
