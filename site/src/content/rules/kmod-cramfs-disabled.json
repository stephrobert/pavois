{
  "id": "kmod-cramfs-disabled",
  "supported_os": [
    "debian12",
    "debian13",
    "fedora",
    "rhel10",
    "rhel8",
    "rhel9",
    "ubuntu2204",
    "ubuntu2404"
  ],
  "os_versions": {
    "debian12": "1.1.0",
    "debian13": "1.0.0",
    "rhel10": "1.0.0",
    "rhel8": "4.0.0",
    "rhel9": "2.0.0",
    "ubuntu2204": "3.0.0",
    "ubuntu2404": "1.0.0"
  },
  "severity": "low",
  "domain": "Kernel modules",
  "evidence_type": "effective-runtime",
  "reboot_survivable": "yes",
  "norms": {
    "cis": "1.1.1.1",
    "nist": [
      "3.4.6",
      "CM-6(a)",
      "CM-7(a)",
      "CM-7(b)"
    ]
  },
  "check": [
    "describe kernel_module('cramfs') do",
    "  it { should_not be_loaded }",
    "  it { should be_disabled }",
    "end"
  ],
  "remediation": {
    "action": "blacklist",
    "name": "cramfs",
    "reboot_required": true,
    "resource": "kernel_module"
  },
  "title": {
    "en": "Disable Mounting of cramfs",
    "fr": "Désactiver le montage de cramfs"
  },
  "needs_translation": [],
  "rationale": {
    "en": "**cramfs** is an obsolete compressed read-only filesystem that modern systems do not need. Its driver still parses on-disk structures, so a crafted cramfs image (e.g. inserted via USB and auto-mounted) could trigger a kernel parsing bug. Removing support for unneeded filesystem types reduces the local attack surface of the server.",
    "fr": "**cramfs** est un système de fichiers compressé en lecture seule obsolète dont les systèmes modernes n'ont pas besoin. Son pilote analyse encore les structures sur disque ; une image cramfs forgée (par ex. insérée via USB et montée automatiquement) pourrait déclencher un bug d'analyse du noyau. Supprimer la prise en charge des types de systèmes de fichiers inutiles réduit la surface d'attaque locale du serveur."
  },
  "summary": {
    "en": "Ensures the `cramfs` filesystem kernel module is not loaded and is blacklisted so cramfs images cannot be mounted.",
    "fr": "Garantit que le module noyau du système de fichiers `cramfs` n'est pas chargé et est mis en liste noire afin qu'aucune image cramfs ne puisse être montée."
  },
  "check_note": {
    "en": "Pavois uses `kernel_module('cramfs')` to assert the module is **not loaded** and is **disabled** in the effective modprobe policy. Inspecting the live kernel plus the resolved modprobe config (not one `.conf`) catches a module blacklisted somewhere but still loaded, or re-enabled by an override drop-in.",
    "fr": "Pavois utilise `kernel_module('cramfs')` pour vérifier que le module n'est **pas chargé** et qu'il est **désactivé** dans la politique modprobe effective. Inspecter le noyau vivant plus la config modprobe résolue (et non un seul `.conf`) détecte un module en liste noire quelque part mais encore chargé, ou réactivé par un override."
  },
  "verify": {
    "en": "Run `lsmod | grep cramfs` (no output = not loaded) and `modprobe -n -v cramfs` (expect `install /bin/true` or `/bin/false`). `modprobe --showconfig | grep cramfs` shows the effective rules.",
    "fr": "Exécutez `lsmod | grep cramfs` (aucune sortie = non chargé) et `modprobe -n -v cramfs` (attendu : `install /bin/true` ou `/bin/false`). `modprobe --showconfig | grep cramfs` affiche les règles effectives."
  },
  "logs": {
    "en": "A blocked mount/load attempt is logged by the kernel: `journalctl -k | grep -i cramfs` / `dmesg | grep -i cramfs`. `lsmod` shows resident modules; `modprobe --showconfig` shows the policy.",
    "fr": "Une tentative de montage/chargement bloquée est journalisée par le noyau : `journalctl -k | grep -i cramfs` / `dmesg | grep -i cramfs`. `lsmod` montre les modules résidents ; `modprobe --showconfig` montre la politique."
  },
  "remediation_note": {
    "en": "`pavois harden apply` uses the `kernel_module` resource to **blacklist** `cramfs`: it writes a modprobe rule (`install cramfs /bin/false` plus a blacklist entry) so the filesystem cannot be auto-loaded on mount, and unloads it if resident. `reboot_required` is true to guarantee removal.",
    "fr": "`pavois harden apply` utilise la ressource `kernel_module` pour mettre `cramfs` en **liste noire** : il écrit une règle modprobe (`install cramfs /bin/false` plus une entrée blacklist) afin que le système de fichiers ne puisse être chargé automatiquement au montage, et le décharge s'il est résident. `reboot_required` est vrai pour garantir le retrait."
  },
  "impact": {
    "en": "Disabling `cramfs` has **no impact** on normal operation. Precaution: a small number of embedded images or some `snap`/initramfs tooling historically used cramfs, verify no mounted filesystem is of type cramfs (`findmnt -t cramfs`, `cat /proc/filesystems | grep cramfs`) before applying. A reboot is needed to unload an already-loaded module.",
    "fr": "Désactiver `cramfs` n'a **aucun impact** sur le fonctionnement normal. Précaution : quelques images embarquées ou certains outils `snap`/initramfs ont historiquement utilisé cramfs, vérifiez qu'aucun système de fichiers monté n'est de type cramfs (`findmnt -t cramfs`, `cat /proc/filesystems | grep cramfs`) avant d'appliquer. Un redémarrage est nécessaire pour décharger un module déjà chargé."
  }
}
