{
  "id": "account-no-empty-passwords",
  "supported_os": [
    "debian12",
    "debian13",
    "fedora",
    "rhel10",
    "rhel8",
    "rhel9",
    "ubuntu2204",
    "ubuntu2404",
    "ubuntu2604"
  ],
  "os_versions": {
    "debian12": "1.1.0",
    "debian13": "1.0.0",
    "rhel10": "1.0.0",
    "rhel8": "4.0.0",
    "rhel9": "2.0.0",
    "ubuntu2204": "3.0.0",
    "ubuntu2404": "1.0.0"
  },
  "severity": "critical",
  "domain": "Accounts",
  "evidence_type": "inventory-state",
  "reboot_survivable": "yes",
  "norms": {
    "cis": [
      "8.3.1",
      "5.3.3.4.1",
      "7.2.2"
    ],
    "nist": [
      "3.1.1",
      "CM-6(a)",
      "IA-5(1)(a)",
      "IA-5(c)"
    ],
    "pci-dss": [
      "2.2.2",
      "8.3.1"
    ],
    "stig": [
      "UBTU-22-611060",
      "UBTU-22-611065",
      "UBTU-24-300028",
      "UBTU-24-300027"
    ]
  },
  "check": [
    "describe command('awk -F: \\'($2==\"\"){print $1}\\' /etc/shadow') do",
    "  its('stdout.strip') { should eq '' }",
    "end"
  ],
  "remediation": {
    "command": "awk -F: '($2==\"\"){print $1}' /etc/shadow   # accounts with NO password\n# passwd -l <user>   (lock) for each, after confirming it should not log in\n",
    "reason": "locking an account is disruptive, confirm each empty-password account before locking",
    "resource": "manual"
  },
  "title": {
    "en": "Prevent Login to Accounts With Empty Password",
    "fr": "Empêcher la connexion aux comptes sans mot de passe"
  },
  "summary": {
    "en": "Ensures no account in `/etc/shadow` has an empty password field.",
    "fr": "Garantit qu'aucun compte de `/etc/shadow` n'a un champ mot de passe vide."
  },
  "rationale": {
    "en": "An account with an empty password hash in `/etc/shadow` can be logged into with **no credentials at all**. Anyone with network or console access can take over that account and run commands with its privileges, and if it is a privileged or service account, the whole system. Empty passwords must never exist in operational environments.",
    "fr": "Un compte dont le hachage de mot de passe est vide dans `/etc/shadow` permet une connexion **sans aucun identifiant**. Quiconque a un accès réseau ou console peut prendre le contrôle de ce compte et exécuter des commandes avec ses privilèges, et s'il s'agit d'un compte privilégié ou de service, du système entier. Les mots de passe vides ne doivent jamais exister en environnement opérationnel."
  },
  "check_note": {
    "en": "Pavois uses `awk` to list any `/etc/shadow` entry whose second field (the password hash) is empty. The check passes only when the list is empty. Reading the effective shadow database is authoritative: it reflects the actual stored credential state, not what a PAM policy is merely supposed to enforce.",
    "fr": "Pavois utilise `awk` pour lister toute entrée de `/etc/shadow` dont le deuxième champ (le hachage du mot de passe) est vide. Le contrôle réussit uniquement si la liste est vide. Lire la base shadow effective fait foi : elle reflète l'état réel des identifiants stockés, et non ce qu'une politique PAM est censée imposer."
  },
  "verify": {
    "en": "Run `awk -F: '($2==\"\"){print $1}' /etc/shadow`. **Expected output: nothing**. Any printed username is an account that can be entered with no password and must be locked or given a password.",
    "fr": "Exécutez `awk -F: '($2==\"\"){print $1}' /etc/shadow`. **Sortie attendue : rien**. Tout nom affiché est un compte accessible sans mot de passe, à verrouiller ou à doter d'un mot de passe."
  },
  "logs": {
    "en": "- List empty-password accounts: `awk -F: '($2==\"\"){print $1}' /etc/shadow`.\n- Confirm lock state: `passwd -S <user>` (look for `L` / `LK`).\n- Successful logins (a flag if they used an empty password): `/var/log/auth.log` or `/var/log/secure`.",
    "fr": "- Lister les comptes sans mot de passe : `awk -F: '($2==\"\"){print $1}' /etc/shadow`.\n- Confirmer le verrouillage : `passwd -S <utilisateur>` (chercher `L` / `LK`).\n- Connexions réussies (alerte si réalisées sans mot de passe) : `/var/log/auth.log` ou `/var/log/secure`."
  },
  "remediation_note": {
    "en": "No automated remediation is shipped, because the right action depends on the account. Fix it manually: lock the account with `passwd -l <user>` (or `usermod -L <user>`) if it should not log in, or set a strong password with `passwd <user>`, then re-run the scan.",
    "fr": "Aucune remédiation automatique n'est fournie, car l'action adéquate dépend du compte. Corrigez manuellement : verrouillez le compte avec `passwd -l <utilisateur>` (ou `usermod -L <utilisateur>`) s'il ne doit pas se connecter, ou définissez un mot de passe fort avec `passwd <utilisateur>`, puis relancez le scan."
  },
  "impact": {
    "en": "An empty password is an immediate, no-credential entry point, one of the highest-impact account flaws (full takeover of the account, often the system). Before locking an account, confirm it is not relied upon by an automated process that authenticates without a password; locking the wrong account can break services or remove your own access.",
    "fr": "Un mot de passe vide est un point d'entrée immédiat sans identifiant, l'une des failles de compte les plus graves (prise de contrôle totale du compte, souvent du système). Avant de verrouiller un compte, vérifiez qu'aucun processus automatisé ne s'y authentifie sans mot de passe ; verrouiller le mauvais compte peut casser des services ou supprimer votre propre accès."
  },
  "datePublished": "2026-06-28",
  "dateModified": "2026-07-14"
}
