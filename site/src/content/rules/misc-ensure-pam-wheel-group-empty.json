{
  "id": "misc-ensure-pam-wheel-group-empty",
  "supported_os": [
    "almalinux9",
    "debian12",
    "debian13",
    "fedora",
    "rhel8",
    "rhel9",
    "ubuntu2204",
    "ubuntu2404"
  ],
  "os_versions": {
    "almalinux9": "2.0.0",
    "debian12": "1.1.0",
    "debian13": "1.0.0",
    "rhel8": "4.0.0",
    "rhel9": "2.0.0",
    "ubuntu2204": "3.0.0",
    "ubuntu2404": "1.0.0"
  },
  "severity": "medium",
  "domain": "Hardening (misc)",
  "evidence_type": "inventory-state",
  "reboot_survivable": "yes",
  "norms": {
    "cis": [
      "5.2.7",
      "2.2.6"
    ],
    "pci-dss": "2.2.6"
  },
  "check": [
    "describe command('g=$(grep -rhoE \\'pam_wheel.so.*group=[a-z]+\\' /etc/pam.d/su 2>/dev/null | grep -oE \\'group=[a-z]+\\' | cut -d= -f2 | head -1); { [ -n \"$g\" ] && [ -z \"$(getent group \"$g\" | cut -d: -f4)\" ] && echo ok; } || echo ko') do",
    "  its('stdout.strip') { should eq 'ok' }",
    "end"
  ],
  "remediation": {},
  "title": {
    "en": "Ensure the Group Used by pam_wheel.so Module Exists on System and is Empty",
    "fr": "S'assurer que le groupe utilisé par le module pam_wheel.so existe et est vide"
  },
  "needs_translation": [],
  "rationale": {
    "en": "The `su` program runs commands with a substitute user and group ID, and is the classic way to become **root**. The `pam_wheel.so` module restricts who may run `su` to members of a named group (e.g. `wheel` or `sugroup`). If that group is configured but **empty**, no unprivileged user can pivot to root through `su` — privilege escalation must go through `sudo`, which is logged and per-command. A populated wheel group silently grants its members an unaudited path to full root.",
    "fr": "Le programme `su` exécute des commandes avec un utilisateur et un groupe de substitution ; c'est la manière classique de devenir **root**. Le module `pam_wheel.so` restreint l'usage de `su` aux membres d'un groupe nommé (par ex. `wheel` ou `sugroup`). Si ce groupe est configuré mais **vide**, aucun utilisateur non privilégié ne peut basculer vers root via `su` : l'élévation passe alors par `sudo`, qui est journalisé et limité à chaque commande. Un groupe wheel peuplé accorde silencieusement à ses membres une voie non auditée vers root."
  },
  "summary": {
    "en": "Ensures the group referenced by `pam_wheel.so` in `/etc/pam.d/su` exists and has **no members**, so no user can escalate to root through `su`.",
    "fr": "Garantit que le groupe référencé par `pam_wheel.so` dans `/etc/pam.d/su` existe et n'a **aucun membre**, afin qu'aucun utilisateur ne puisse devenir root via `su`."
  },
  "check_note": {
    "en": "Pavois reads the **effective** PAM configuration: it extracts the `group=` argument actually passed to `pam_wheel.so` in `/etc/pam.d/su`, then resolves that group through `getent group` (NSS) and checks its member field is empty. Resolving via `getent` rather than only parsing `/etc/group` is what catches members coming from **LDAP, SSSD or other NSS backends** that a flat file scan would miss.",
    "fr": "Pavois lit la configuration PAM **effective** : il extrait l'argument `group=` réellement passé à `pam_wheel.so` dans `/etc/pam.d/su`, résout ce groupe via `getent group` (NSS) et vérifie que le champ des membres est vide. Résoudre via `getent` plutôt que de seulement analyser `/etc/group` permet de détecter des membres provenant de **LDAP, SSSD ou d'autres backends NSS** qu'une simple lecture de fichier manquerait."
  },
  "verify": {
    "en": "Find the group enforced by PAM and list its members:\n\n- `grep -E 'pam_wheel.so.*group=' /etc/pam.d/su` — read the configured group name (e.g. `group=sugroup`).\n- `getent group sugroup` — expected output ends with an **empty** member field, e.g. `sugroup:x:1001:` (nothing after the last colon).",
    "fr": "Trouvez le groupe imposé par PAM et listez ses membres :\n\n- `grep -E 'pam_wheel.so.*group=' /etc/pam.d/su` — lire le nom de groupe configuré (par ex. `group=sugroup`).\n- `getent group sugroup` — la sortie attendue se termine par un champ de membres **vide**, par ex. `sugroup:x:1001:` (rien après le dernier deux-points)."
  },
  "logs": {
    "en": "PAM `su` events are logged by the authentication stack:\n\n- `journalctl -t su` or `grep ' su' /var/log/auth.log` (Debian/Ubuntu) / `/var/log/secure` (RHEL) — shows `pam_wheel(su:auth): Access denied ...` when a non-member is correctly rejected.\n- Successful escalations appear as `su: (to root) <user> on pts/N`.",
    "fr": "Les événements `su` de PAM sont journalisés par la pile d'authentification :\n\n- `journalctl -t su` ou `grep ' su' /var/log/auth.log` (Debian/Ubuntu) / `/var/log/secure` (RHEL) — affiche `pam_wheel(su:auth): Access denied ...` lorsqu'un non-membre est correctement rejeté.\n- Les élévations réussies apparaissent comme `su: (to root) <user> on pts/N`."
  },
  "remediation_note": {
    "en": "No automated harden plan is defined for this rule yet, so it must be applied **manually**: identify the group named in `/etc/pam.d/su` (`group=`), then remove every member with `gpasswd --delete <user> <group>` (or edit the group's member list) until `getent group <group>` shows no members. Ensure the group still exists.",
    "fr": "Aucun plan de durcissement automatisé n'est défini pour cette règle ; elle doit être appliquée **manuellement** : identifiez le groupe nommé dans `/etc/pam.d/su` (`group=`), puis retirez chaque membre avec `gpasswd --delete <user> <group>` (ou modifiez la liste des membres du groupe) jusqu'à ce que `getent group <group>` n'affiche plus de membres. Vérifiez que le groupe existe toujours."
  },
  "impact": {
    "en": "**Risk if not applied:** members of the wheel/su group hold an unaudited, full-root escape hatch via `su` even when `sudo` policy is tight.\n\n**Precautions before applying:** before emptying the group, confirm that every administrator can still reach root another way — typically a working `sudo` rule. Removing the last member while `sudo` is broken or while `pam_wheel.so` is also enforced on the `sudo` stack can lock all admins out of privilege escalation. Keep a root console session open while you make the change.",
    "fr": "**Risque si non appliqué :** les membres du groupe wheel/su disposent d'une porte de sortie non auditée vers root complet via `su`, même si la politique `sudo` est stricte.\n\n**Précautions avant application :** avant de vider le groupe, vérifiez que chaque administrateur peut encore atteindre root autrement — typiquement une règle `sudo` fonctionnelle. Retirer le dernier membre alors que `sudo` est cassé ou que `pam_wheel.so` est aussi imposé sur la pile `sudo` peut priver tous les administrateurs d'élévation de privilèges. Gardez une session console root ouverte pendant la modification."
  }
}
