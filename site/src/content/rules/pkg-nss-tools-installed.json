{
  "id": "pkg-nss-tools-installed",
  "supported_os": [
    "rhel9"
  ],
  "os_versions": {
    "rhel9": "2.0.0"
  },
  "severity": "medium",
  "domain": "Packages",
  "evidence_type": "inventory-state",
  "reboot_survivable": "yes",
  "norms": {},
  "check": [
    "describe package('nss-tools') do",
    "  it { should be_installed }",
    "end"
  ],
  "remediation": {
    "action": "install",
    "name": "nss-tools",
    "resource": "package"
  },
  "title": {
    "en": "Ensure nss-tools is installed",
    "fr": "Garantir que nss-tools est installé"
  },
  "needs_translation": [],
  "rationale": {
    "en": "**Network Security Services (NSS)** is a set of libraries for cross-platform development of security-enabled client and server applications. The **`nss-tools`** package installs the command-line utilities (`certutil`, `modutil`, `pk12util`, etc.) needed to manage the NSS certificate and key databases. Without these tools, administrators cannot inspect, import, or maintain the certificates that many RHEL services rely on, hampering certificate hygiene and incident response.",
    "fr": "**Network Security Services (NSS)** est un ensemble de bibliothèques pour le développement multiplateforme d'applications client/serveur sécurisées. Le paquet **`nss-tools`** installe les utilitaires en ligne de commande (`certutil`, `modutil`, `pk12util`, etc.) nécessaires pour gérer les bases de certificats et de clés NSS. Sans ces outils, les administrateurs ne peuvent ni inspecter, ni importer, ni maintenir les certificats dont dépendent de nombreux services RHEL, ce qui nuit à l'hygiène des certificats et à la réponse aux incidents."
  },
  "summary": {
    "en": "Ensures the **`nss-tools`** package (NSS certificate/key database command-line tools) is **installed**.",
    "fr": "Garantit que le paquet **`nss-tools`** (outils en ligne de commande des bases de certificats/clés NSS) est **installé**."
  },
  "check_note": {
    "en": "Pavois asks the package manager whether **`nss-tools`** is installed via the InSpec `package` resource (effectively `rpm -q nss-tools`). Querying installed-package state directly reflects which binaries are actually available on the host, which is more reliable than probing for individual tool paths that vary across releases.",
    "fr": "Pavois interroge le gestionnaire de paquets pour savoir si **`nss-tools`** est installé via la ressource InSpec `package` (équivalent à `rpm -q nss-tools`). Interroger directement l'état des paquets installés reflète quels binaires sont réellement disponibles sur l'hôte, ce qui est plus fiable que de chercher des chemins d'outils individuels qui varient d'une version à l'autre."
  },
  "verify": {
    "en": "Run `rpm -q nss-tools`. The expected output is a versioned package name such as `nss-tools-3.90.0-...`. The CLI tools are then available, e.g. `certutil -H`.",
    "fr": "Exécutez `rpm -q nss-tools`. La sortie attendue est un nom de paquet versionné tel que `nss-tools-3.90.0-...`. Les outils CLI sont alors disponibles, par exemple `certutil -H`."
  },
  "logs": {
    "en": "- `rpm -q nss-tools`, confirms the package is installed.\n- `rpm -qi nss-tools`, shows install date and source repository.\n- `certutil -L -d <nssdb>`, lists certificates in a given NSS database (proves the tools work).",
    "fr": "- `rpm -q nss-tools`, confirme que le paquet est installé.\n- `rpm -qi nss-tools`, affiche la date d'installation et le dépôt source.\n- `certutil -L -d <nssdb>`, liste les certificats d'une base NSS donnée (prouve que les outils fonctionnent)."
  },
  "remediation_note": {
    "en": "Pavois's harden plan declares a `package` resource for **`nss-tools`** with action **install** and applies it with `pavois harden apply`, which runs `dnf install nss-tools` under the hood. No service restart is required.",
    "fr": "Le plan de durcissement de Pavois déclare une ressource `package` pour **`nss-tools`** avec l'action **install** et l'applique avec `pavois harden apply`, ce qui exécute `dnf install nss-tools` en arrière-plan. Aucun redémarrage de service n'est requis."
  },
  "impact": {
    "en": "Installing `nss-tools` is non-disruptive: it only adds command-line utilities and does not change any running service. There is essentially **no rollback risk**. The only consideration is the minor disk footprint and ensuring the package comes from a trusted repository.",
    "fr": "Installer `nss-tools` est non perturbateur : cela ajoute seulement des utilitaires en ligne de commande et ne modifie aucun service en cours d'exécution. Il n'y a pratiquement **aucun risque de retour arrière**. La seule considération est la faible empreinte disque et le fait de s'assurer que le paquet provient d'un dépôt de confiance."
  }
}