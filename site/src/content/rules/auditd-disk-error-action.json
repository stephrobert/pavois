{
  "id": "auditd-disk-error-action",
  "supported_os": [
    "debian12",
    "debian13",
    "fedora",
    "rhel10",
    "rhel8",
    "rhel9",
    "ubuntu2204",
    "ubuntu2404",
    "ubuntu2604"
  ],
  "os_versions": {
    "debian12": "1.1.0",
    "debian13": "1.0.0",
    "rhel10": "1.0.0",
    "rhel8": "4.0.0",
    "rhel9": "2.0.0",
    "ubuntu2204": "3.0.0",
    "ubuntu2404": "1.0.0"
  },
  "severity": "medium",
  "domain": "Audit (auditd daemon)",
  "evidence_type": "persistent-config",
  "reboot_survivable": "yes",
  "norms": {
    "cis": [
      "6.2.2.3",
      "6.3.2.3"
    ],
    "nist": [
      "AU-5(1)",
      "AU-5(2)",
      "AU-5(4)",
      "AU-5(b)",
      "CM-6(a)"
    ]
  },
  "check": [
    "describe command('grep -qiE \\'^[[:space:]]*disk_error_action[[:space:]]*=[[:space:]]*(syslog|single|halt)\\b\\' /etc/audit/auditd.conf 2>/dev/null && echo ok || echo ko') do",
    "  its('stdout.strip') { should eq 'ok' }",
    "end"
  ],
  "remediation": {
    "file": "/etc/audit/auditd.conf",
    "key": "disk_error_action",
    "resource": "conf_line",
    "service": "auditd",
    "value": "syslog"
  },
  "title": {
    "en": "Configure auditd Disk Error Action on Disk Error",
    "fr": "Configurer l'action auditd en cas d'erreur disque (disk_error_action)"
  },
  "summary": {
    "en": "Sets `disk_error_action` in `/etc/audit/auditd.conf` to a defined reaction (**`syslog`**, **`single`** or **`halt`**) when auditd hits a disk write error.",
    "fr": "Définit `disk_error_action` dans `/etc/audit/auditd.conf` sur une réaction définie (**`syslog`**, **`single`** ou **`halt`**) lorsqu'auditd rencontre une erreur d'écriture disque."
  },
  "rationale": {
    "en": "Taking appropriate action in case of disk errors will minimize the possibility of losing audit records.",
    "fr": "Prendre une mesure appropriée en cas d'erreur disque minimise le risque de perdre des enregistrements d'audit."
  },
  "check_note": {
    "en": "Pavois greps `/etc/audit/auditd.conf` for `disk_error_action = syslog|single|halt` and expects `ok`. Reading the daemon's effective config shows the reaction auditd will actually perform on a disk error, independent of compile-time defaults.",
    "fr": "Pavois recherche `disk_error_action = syslog|single|halt` dans `/etc/audit/auditd.conf` et attend `ok`. Lire la configuration effective du démon montre la réaction réellement effectuée par auditd lors d'une erreur disque, indépendamment des valeurs par défaut de compilation."
  },
  "verify": {
    "en": "Run `grep -i disk_error_action /etc/audit/auditd.conf`. Expected: `disk_error_action = syslog` (or `single`/`halt`). Reload auditd to apply.",
    "fr": "Exécutez `grep -i disk_error_action /etc/audit/auditd.conf`. Attendu : `disk_error_action = syslog` (ou `single`/`halt`). Rechargez auditd pour appliquer."
  },
  "logs": {
    "en": "- With `syslog`, errors land in the system journal / `/var/log/syslog` (`journalctl -u auditd`).\n- `/var/log/audit/audit.log` and `dmesg` corroborate the underlying disk fault.",
    "fr": "- Avec `syslog`, les erreurs apparaissent dans le journal système / `/var/log/syslog` (`journalctl -u auditd`).\n- `/var/log/audit/audit.log` et `dmesg` corroborent la panne disque sous-jacente."
  },
  "remediation_note": {
    "en": "Pavois's harden plan uses the **`conf_line`** resource to set `disk_error_action = syslog` in `/etc/audit/auditd.conf`, then notifies the **`auditd`** service to reload. `syslog` is chosen as the least disruptive defined reaction. Apply with `pavois harden apply`.",
    "fr": "Le plan de durcissement de Pavois utilise la ressource **`conf_line`** pour définir `disk_error_action = syslog` dans `/etc/audit/auditd.conf`, puis notifie le service **`auditd`** pour le recharger. `syslog` est retenu comme la réaction définie la moins perturbatrice. Appliquez avec `pavois harden apply`."
  },
  "impact": {
    "en": "Without a defined action, a disk error can silently drop audit records, creating gaps in the trail. `syslog` is low-risk (it only logs); choosing `single` or `halt` instead stops the host on error, appropriate for high-assurance systems but capable of interrupting production, so reserve those for hosts where losing audit data is unacceptable and ensure console access first.",
    "fr": "Sans action définie, une erreur disque peut faire disparaître silencieusement des enregistrements d'audit, créant des trous dans la piste. `syslog` est peu risqué (il se contente de journaliser) ; choisir `single` ou `halt` arrête l'hôte en cas d'erreur, pertinent pour les systèmes à haute assurance mais susceptible d'interrompre la production : réservez-les aux hôtes où la perte de données d'audit est inacceptable et assurez-vous d'abord d'un accès console."
  },
  "datePublished": "2026-06-28",
  "dateModified": "2026-07-14"
}
