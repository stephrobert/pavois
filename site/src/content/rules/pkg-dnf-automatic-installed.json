{
  "id": "pkg-dnf-automatic-installed",
  "supported_os": [
    "almalinux9",
    "fedora",
    "rhel8",
    "rhel9"
  ],
  "os_versions": {
    "almalinux9": "2.0.0",
    "rhel8": "4.0.0",
    "rhel9": "2.0.0"
  },
  "severity": "medium",
  "domain": "Packages",
  "evidence_type": "inventory-state",
  "reboot_survivable": "yes",
  "norms": {
    "bp28": "R61"
  },
  "check": [
    "describe package('dnf-automatic') do",
    "  it { should be_installed }",
    "end"
  ],
  "remediation": {
    "action": "install",
    "name": "dnf-automatic",
    "resource": "package"
  },
  "title": {
    "en": "Install dnf-automatic Package",
    "fr": "Installer le paquet dnf-automatic"
  },
  "needs_translation": [],
  "rationale": {
    "en": "dnf-automatic is an alternative command-line interface to `dnf upgrade` suitable for automatic, regular execution, ensuring security updates are applied promptly without manual intervention.",
    "fr": "dnf-automatic est une interface en ligne de commande alternative à `dnf upgrade`, adaptée à une exécution automatique et régulière, garantissant l'application rapide des mises à jour de sécurité sans intervention manuelle."
  },
  "summary": {
    "en": "Ensures the **dnf-automatic** package is installed so security updates can be downloaded and applied automatically on a schedule.",
    "fr": "Garantit que le paquet **dnf-automatic** est installé afin que les mises à jour de sécurité puissent être téléchargées et appliquées automatiquement selon un calendrier."
  },
  "check_note": {
    "en": "Pavois verifies that the `dnf-automatic` package is present via `rpm`. Automatic patching closes the window between disclosure and remediation; the package provides the systemd timer that drives it. (Installation makes the capability available — you still configure `/etc/dnf/automatic.conf` and enable the timer.)",
    "fr": "Pavois vérifie que le paquet `dnf-automatic` est présent via `rpm`. Le correctif automatique réduit le délai entre la divulgation et la remédiation ; le paquet fournit le timer systemd qui le pilote. (L'installation rend la capacité disponible — vous devez encore configurer `/etc/dnf/automatic.conf` et activer le timer.)"
  },
  "verify": {
    "en": "Run `rpm -q dnf-automatic`, which should return an installed version. Confirm the scheduled run with `systemctl is-enabled dnf-automatic.timer` (expected `enabled`) and `systemctl list-timers dnf-automatic.timer`.",
    "fr": "Exécutez `rpm -q dnf-automatic`, qui doit renvoyer une version installée. Confirmez l'exécution planifiée avec `systemctl is-enabled dnf-automatic.timer` (attendu : `enabled`) et `systemctl list-timers dnf-automatic.timer`."
  },
  "logs": {
    "en": "Confirm the package with `rpm -q dnf-automatic`; automatic update runs are logged via `journalctl -u dnf-automatic.service` and the timer state via `systemctl list-timers`.",
    "fr": "Confirmez le paquet avec `rpm -q dnf-automatic` ; les exécutions de mise à jour automatique sont journalisées via `journalctl -u dnf-automatic.service` et l'état du timer via `systemctl list-timers`."
  },
  "remediation_note": {
    "en": "Pavois's harden plan installs the `dnf-automatic` package (resource `package`, action `install`) and is applied with `pavois harden apply`. After installation, configure `/etc/dnf/automatic.conf` and enable `dnf-automatic.timer` so updates actually run.",
    "fr": "Le plan de durcissement de Pavois installe le paquet `dnf-automatic` (ressource `package`, action `install`) et s'applique avec `pavois harden apply`. Après l'installation, configurez `/etc/dnf/automatic.conf` et activez `dnf-automatic.timer` pour que les mises à jour s'exécutent réellement."
  },
  "impact": {
    "en": "Without automatic updates, hosts stay vulnerable until someone patches manually. Installing the package is non-disruptive. Be aware that enabling unattended *apply* (rather than download-only) can restart services or, rarely, introduce regressions — on critical systems prefer staged rollouts and maintenance windows, and review `/etc/dnf/automatic.conf` before enabling apply mode.",
    "fr": "Sans mises à jour automatiques, les hôtes restent vulnérables jusqu'à un correctif manuel. L'installation du paquet est sans interruption. Notez que l'activation de l'*application* non surveillée (plutôt que le simple téléchargement) peut redémarrer des services ou, rarement, introduire des régressions — sur les systèmes critiques, privilégiez des déploiements échelonnés et des fenêtres de maintenance, et examinez `/etc/dnf/automatic.conf` avant d'activer le mode application."
  }
}
