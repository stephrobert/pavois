{
  "id": "pkg-bind-removed",
  "supported_os": [
    "almalinux9",
    "debian12",
    "debian13",
    "fedora",
    "rhel8",
    "rhel9",
    "ubuntu2204",
    "ubuntu2404"
  ],
  "os_versions": {
    "almalinux9": "2.0.0",
    "debian12": "1.1.0",
    "debian13": "1.0.0",
    "rhel8": "4.0.0",
    "rhel9": "2.0.0",
    "ubuntu2204": "3.0.0",
    "ubuntu2404": "1.0.0"
  },
  "severity": "low",
  "domain": "Packages",
  "evidence_type": "inventory-state",
  "reboot_survivable": "yes",
  "norms": {
    "cis": [
      "2.1.4",
      "2.1.5"
    ],
    "nist": [
      "CM-6(a)",
      "CM-7(a)",
      "CM-7(b)"
    ]
  },
  "check": [
    "describe package('bind') do",
    "  it { should_not be_installed }",
    "end"
  ],
  "remediation": {
    "action": "remove",
    "name": "bind",
    "resource": "package"
  },
  "title": {
    "en": "Uninstall bind Package",
    "fr": "Desinstaller le paquet bind"
  },
  "needs_translation": [],
  "rationale": {
    "en": "**BIND** (`named`) is a full DNS server that listens on the network and has a long history of high-severity vulnerabilities. If the host is not meant to serve DNS, having the package installed adds an unnecessary, potentially internet-facing service that an attacker could start or exploit. Removing it enforces least functionality (CIS 2.1.4/2.1.5, NIST CM-7).",
    "fr": "**BIND** (`named`) est un serveur DNS complet qui ecoute sur le reseau et possede un long historique de vulnerabilites a haute severite. Si l'hote n'est pas destine a servir le DNS, la presence du paquet ajoute un service inutile, potentiellement expose a internet, qu'un attaquant pourrait demarrer ou exploiter. Le supprimer applique le principe de moindre fonctionnalite (CIS 2.1.4/2.1.5, NIST CM-7)."
  },
  "summary": {
    "en": "Ensures the **BIND** DNS server package is **not installed** unless the host is intended to be a DNS server.",
    "fr": "Garantit que le paquet serveur DNS **BIND** n'est **pas installe**, sauf si l'hote est destine a etre un serveur DNS."
  },
  "check_note": {
    "en": "Pavois asks the package database (via `dpkg`/`rpm`) whether `bind` is installed and expects it absent. Checking the **effective package state** is authoritative regardless of whether the `named` service is currently stopped or masked.",
    "fr": "Pavois interroge la base de paquets (via `dpkg`/`rpm`) pour savoir si `bind` est installe et l'attend absent. Verifier l'**etat effectif des paquets** fait autorite, que le service `named` soit actuellement arrete ou masque."
  },
  "verify": {
    "en": "Run `rpm -q bind` (RHEL-family) or `dpkg -s bind9` (Debian/Ubuntu, where the package is `bind9`), expected not installed. Confirm nothing listens on port 53 with `ss -lntup | grep ':53'` (expected no `named`).",
    "fr": "Executez `rpm -q bind` (famille RHEL) ou `dpkg -s bind9` (Debian/Ubuntu, ou le paquet se nomme `bind9`), attendu : non installe. Verifiez que rien n'ecoute sur le port 53 avec `ss -lntup | grep ':53'` (attendu : pas de `named`)."
  },
  "logs": {
    "en": "- `rpm -q bind` / `dpkg -l bind9` confirm removal.\n- While installed, the DNS server logs to `journalctl -u named` (RHEL) or `journalctl -u named` / `bind9` (Debian) and its listening socket is visible with `ss -lntup` (port 53).",
    "fr": "- `rpm -q bind` / `dpkg -l bind9` confirment la suppression.\n- Tant qu'il est installe, le serveur DNS journalise dans `journalctl -u named` (RHEL) ou `journalctl -u named` / `bind9` (Debian) et sa socket d'ecoute est visible avec `ss -lntup` (port 53)."
  },
  "remediation_note": {
    "en": "Pavois's harden plan removes the `bind` package (`package` resource, `remove` action) with `pavois harden apply`, stopping and disabling the `named` service in the process.",
    "fr": "Le plan de durcissement de Pavois supprime le paquet `bind` (ressource `package`, action `remove`) avec `pavois harden apply`, arretant et desactivant le service `named` au passage."
  },
  "impact": {
    "en": "Removing BIND breaks the host as a DNS server, do **not** apply this on an authoritative or caching DNS server. Before applying, confirm the host is not serving DNS to other systems (check `ss` on port 53 and downstream resolver configs). Note this control targets the DNS *server*; it does not affect the system's own resolver (`systemd-resolved`/`resolv.conf`).",
    "fr": "Supprimer BIND casse l'hote en tant que serveur DNS, n'appliquez **pas** ceci sur un serveur DNS faisant autorite ou en cache. Avant d'appliquer, verifiez que l'hote ne sert pas le DNS a d'autres systemes (controlez `ss` sur le port 53 et les configurations des resolveurs en aval). Notez que ce controle vise le *serveur* DNS ; il n'affecte pas le resolveur propre du systeme (`systemd-resolved`/`resolv.conf`)."
  }
}