{
  "id": "fileperm-backup-etc-group",
  "supported_os": [
    "debian12",
    "debian13",
    "fedora",
    "rhel10",
    "rhel8",
    "rhel9",
    "ubuntu2204",
    "ubuntu2404",
    "ubuntu2604"
  ],
  "os_versions": {
    "debian12": "1.1.0",
    "debian13": "1.0.0",
    "rhel10": "1.0.0",
    "rhel8": "4.0.0",
    "rhel9": "2.0.0",
    "ubuntu2204": "3.0.0",
    "ubuntu2404": "1.0.0"
  },
  "severity": "medium",
  "domain": "File permissions",
  "evidence_type": "inventory-state",
  "reboot_survivable": "yes",
  "norms": {
    "cis": [
      "2.2.6",
      "7.1.4"
    ],
    "nist": "AC-6 (1)",
    "pci-dss": "2.2.6"
  },
  "check": [
    "only_if { file('/etc/group-').exist? }",
    "describe file('/etc/group-') do",
    "  it { should_not be_executable.by('owner') }",
    "  it { should_not be_setuid }",
    "  it { should_not be_executable.by('group') }",
    "  it { should_not be_writable.by('group') }",
    "  it { should_not be_setgid }",
    "  it { should_not be_executable.by('other') }",
    "  it { should_not be_writable.by('other') }",
    "  it { should_not be_sticky }",
    "end"
  ],
  "remediation": {
    "mode": "0644",
    "path": "/etc/group-",
    "resource": "file"
  },
  "title": {
    "en": "Verify Permissions on Backup group File",
    "fr": "Verifier les permissions du fichier de sauvegarde group"
  },
  "needs_translation": [],
  "summary": {
    "en": "Ensures the backup file `/etc/group-` is not group/other-writable and carries no executable or special (setuid/setgid/sticky) bits, i.e. mode `0644` or stricter.",
    "fr": "Garantit que le fichier de sauvegarde `/etc/group-` n'est pas modifiable par le groupe/les autres et ne porte aucun bit executable ou special (setuid/setgid/sticky), soit le mode `0644` ou plus strict."
  },
  "rationale": {
    "en": "`/etc/group-` is the backup copy of `/etc/group` and lists every group and its membership, including administrative groups such as `sudo`, `wheel` and `adm`. If it is group/other-writable, an attacker could **add their account to a privileged group** in the backup; should `/etc/group` ever be restored from it, that membership becomes live, a stealthy path to privilege escalation. Mode `0644` or stricter, owned by `root`, keeps the backup tamper-resistant.",
    "fr": "`/etc/group-` est la copie de sauvegarde de `/etc/group` et liste chaque groupe et ses membres, y compris les groupes d'administration comme `sudo`, `wheel` et `adm`. S'il est modifiable par le groupe/les autres, un attaquant pourrait **ajouter son compte a un groupe privilegie** dans la sauvegarde ; si `/etc/group` est un jour restaure depuis celle-ci, cette appartenance devient effective, une voie furtive vers l'escalade de privileges. Le mode `0644` ou plus strict, appartenant a `root`, garde la sauvegarde resistante a la falsification."
  },
  "check_note": {
    "en": "Pavois reads the **live mode bits** of `/etc/group-` via the InSpec `file` resource, the permissions the kernel enforces, rather than the default the package would set. Backup files are easy to overlook and are sometimes left world-writable by ad-hoc copies; inspecting the real inode catches that. The `only_if` guard skips the check when no backup exists yet.",
    "fr": "Pavois lit les **bits de mode reels** de `/etc/group-` via la ressource InSpec `file`, les permissions appliquees par le noyau, plutot que la valeur par defaut du paquet. Les fichiers de sauvegarde sont faciles a oublier et parfois laisses modifiables par tous lors de copies ad hoc ; inspecter l'inode reel le detecte. La garde `only_if` ignore le controle si aucune sauvegarde n'existe encore."
  },
  "verify": {
    "en": "Run `stat -c '%a %U %G %n' /etc/group-`. Expected output is `644 root root /etc/group-` (or stricter, e.g. `600`).",
    "fr": "Executez `stat -c '%a %U %G %n' /etc/group-`. La sortie attendue est `644 root root /etc/group-` (ou plus strict, par exemple `600`)."
  },
  "logs": {
    "en": "These backups are regenerated by user/group management tools (`useradd`, `groupadd`, `vipw`, `vigr`); their actions show up in `/var/log/auth.log` (Debian/Ubuntu) or `/var/log/secure` (RHEL family). Permission changes can be tracked with an auditd watch and `grep group- /var/log/audit/audit.log`.",
    "fr": "Ces sauvegardes sont regenerees par les outils de gestion d'utilisateurs/groupes (`useradd`, `groupadd`, `vipw`, `vigr`) ; leurs actions apparaissent dans `/var/log/auth.log` (Debian/Ubuntu) ou `/var/log/secure` (famille RHEL). Les changements de permissions peuvent etre suivis via une regle auditd et `grep group- /var/log/audit/audit.log`."
  },
  "remediation_note": {
    "en": "No automated remediation is defined, so apply it **manually**: `chown root:root /etc/group- && chmod u-x,g-wx,o-wx,a-s /etc/group-` (a plain `chmod 644 /etc/group-` reaches the target mode).",
    "fr": "Aucune remediation automatique n'est definie ; appliquez-la **manuellement** : `chown root:root /etc/group- && chmod u-x,g-wx,o-wx,a-s /etc/group-` (un simple `chmod 644 /etc/group-` atteint le mode cible)."
  },
  "impact": {
    "en": "Risk if not applied: a tampered group backup can seed privileged group membership on restore. Precautions: tightening these permissions is safe, `/etc/group-` is only read by administrative tooling running as root, and the world-readable default (`0644`) is acceptable since group names are not secret. There is no service dependency and no lockout risk.",
    "fr": "Risque en cas de non-application : une sauvegarde de groupe falsifiee peut implanter une appartenance a un groupe privilegie lors d'une restauration. Precautions : durcir ces permissions est sans danger, `/etc/group-` n'est lu que par l'outillage d'administration s'executant en root, et la valeur par defaut lisible par tous (`0644`) est acceptable car les noms de groupes ne sont pas secrets. Aucune dependance de service ni risque de blocage."
  }
}
