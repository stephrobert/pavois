{
  "id": "pwquality-lcredit",
  "supported_os": [
    "debian12",
    "debian13",
    "fedora",
    "rhel8",
    "rhel9",
    "ubuntu2204",
    "ubuntu2404"
  ],
  "os_versions": {
    "debian12": "1.1.0",
    "debian13": "1.0.0",
    "rhel8": "4.0.0",
    "rhel9": "2.0.0",
    "ubuntu2204": "3.0.0",
    "ubuntu2404": "1.0.0"
  },
  "severity": "medium",
  "domain": "Passwords (pwquality)",
  "evidence_type": "persistent-config",
  "reboot_survivable": "yes",
  "norms": {
    "bp28": "R31",
    "nist": [
      "CM-6(a)",
      "IA-5(1)(a)",
      "IA-5(4)",
      "IA-5(c)"
    ],
    "pci-dss": [
      "8.3.6",
      "Req-8.2.3"
    ],
    "cis": [
      "5.3.3.2.3",
      "8.3.6"
    ],
    "stig": [
      "UBTU-22-611015",
      "UBTU-24-400270"
    ]
  },
  "check": [
    "describe command('v=$(grep -rh \\'^[[:space:]]*lcredit[[:space:]]*=\\' /etc/security/pwquality.conf /etc/security/pwquality.conf.d/ 2>/dev/null | tail -1 | grep -oE \\'[-]?[0-9]+\\'); { [ -n \"$v\" ] && [ \"$v\" -le -1 ] && echo ok; } || echo ko') do",
    "  its('stdout.strip') { should eq 'ok' }",
    "end"
  ],
  "remediation": {
    "file": "/etc/security/pwquality.conf.d/99-pavois.conf",
    "key": "lcredit",
    "resource": "keyval",
    "value": "-1"
  },
  "title": {
    "en": "Ensure PAM Enforces Password Requirements - Minimum Lowercase Characters",
    "fr": "Garantir que PAM impose des exigences de mot de passe - Minimum de caracteres minuscules"
  },
  "needs_translation": [],
  "rationale": {
    "en": "A complex password increases the time and resources needed to compromise it. The `lcredit` parameter controls how lowercase letters contribute to a password's strength: a value of `-1` means **at least one lowercase character is mandatory**. Requiring lowercase letters enlarges the character search space, making brute-force and dictionary attacks measurably harder. Without it, users can pick passwords drawn from a narrow alphabet that fall quickly to guessing attacks.",
    "fr": "Un mot de passe complexe augmente le temps et les ressources necessaires pour le compromettre. Le parametre `lcredit` controle la contribution des minuscules a la robustesse du mot de passe : une valeur de `-1` signifie **qu'au moins une minuscule est obligatoire**. Exiger des minuscules elargit l'espace de recherche des caracteres, rendant les attaques par force brute et par dictionnaire nettement plus difficiles. Sans cela, les utilisateurs peuvent choisir des mots de passe tires d'un alphabet etroit qui cedent vite aux attaques par devinette."
  },
  "summary": {
    "en": "Forces `pam_pwquality` to require at least one lowercase letter in every new password by setting `lcredit = -1`.",
    "fr": "Force `pam_pwquality` a exiger au moins une lettre minuscule dans chaque nouveau mot de passe en posant `lcredit = -1`."
  },
  "check_note": {
    "en": "Pavois reads the **effective** `lcredit` value by scanning `/etc/security/pwquality.conf` **and** every drop-in under `/etc/security/pwquality.conf.d/`, keeping the **last** definition that actually wins at runtime. A file-based scanner that only inspects the main `pwquality.conf` would miss a value overridden (or set) by a drop-in, producing a false result. The control passes only when the resolved value is `<= -1`.",
    "fr": "Pavois lit la valeur **effective** de `lcredit` en analysant `/etc/security/pwquality.conf` **et** chaque drop-in sous `/etc/security/pwquality.conf.d/`, en conservant la **derniere** definition qui l'emporte reellement a l'execution. Un scanner base sur les fichiers qui n'inspecte que le `pwquality.conf` principal manquerait une valeur redefinie (ou posee) par un drop-in, produisant un faux resultat. Le controle passe uniquement quand la valeur resolue est `<= -1`."
  },
  "verify": {
    "en": "Run `grep -rh '^[[:space:]]*lcredit' /etc/security/pwquality.conf /etc/security/pwquality.conf.d/ | tail -1`. The effective line must read `lcredit = -1` (or any value `<= -1`).",
    "fr": "Executez `grep -rh '^[[:space:]]*lcredit' /etc/security/pwquality.conf /etc/security/pwquality.conf.d/ | tail -1`. La ligne effective doit indiquer `lcredit = -1` (ou toute valeur `<= -1`)."
  },
  "logs": {
    "en": "Password-change attempts that fail complexity are logged via PAM in `/var/log/auth.log` (Debian/Ubuntu) or `/var/log/secure` (RHEL family); look for `pam_pwquality` messages such as **\"The password does not contain enough character classes\"**. Inspect the live config with `grep -r lcredit /etc/security/pwquality.conf.d/`.",
    "fr": "Les tentatives de changement de mot de passe qui echouent a la complexite sont journalisees via PAM dans `/var/log/auth.log` (Debian/Ubuntu) ou `/var/log/secure` (famille RHEL) ; cherchez les messages `pam_pwquality` comme **\"The password does not contain enough character classes\"**. Inspectez la config active avec `grep -r lcredit /etc/security/pwquality.conf.d/`."
  },
  "remediation_note": {
    "en": "`pavois harden apply` writes `lcredit = -1` to the drop-in `/etc/security/pwquality.conf.d/99-Pavois.conf` using the `keyval` resource (an idempotent key/value editor). The high `99-` prefix guarantees it overrides any earlier setting. No service restart is needed; the change applies to the next password change.",
    "fr": "`pavois harden apply` ecrit `lcredit = -1` dans le drop-in `/etc/security/pwquality.conf.d/99-Pavois.conf` via la ressource `keyval` (un editeur cle/valeur idempotent). Le prefixe eleve `99-` garantit qu'il surcharge tout reglage anterieur. Aucun redemarrage de service n'est requis ; le changement s'applique au prochain changement de mot de passe."
  },
  "impact": {
    "en": "Existing passwords are **not** invalidated; the rule only constrains **new** passwords. Users who try to set a password with no lowercase letter will be rejected at change time. Risk of lockout is low, but before applying ensure your automated provisioning or password-rotation scripts generate compliant passwords (at least one lowercase letter), otherwise account creation could fail.",
    "fr": "Les mots de passe existants ne sont **pas** invalides ; la regle ne contraint que les **nouveaux** mots de passe. Les utilisateurs qui tentent de definir un mot de passe sans minuscule seront rejetes au moment du changement. Le risque de blocage est faible, mais avant d'appliquer, assurez-vous que vos scripts de provisionnement ou de rotation de mots de passe generent des mots de passe conformes (au moins une minuscule), sinon la creation de comptes pourrait echouer."
  }
}
