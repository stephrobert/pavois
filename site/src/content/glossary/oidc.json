{
  "term": "OpenID Connect",
  "slug": "oidc",
  "category": "securite",
  "tags": [
    "security",
    "authentication",
    "identity"
  ],
  "aliases": [],
  "related": [
    "oauth",
    "authentication",
    "sso",
    "jwt"
  ],
  "fr": {
    "translation": "",
    "short": "Couche d'authentification construite sur oauth 2.0 permettant vérification identité utilisateur via tokens standardisés",
    "full": "**OpenID Connect** (OIDC) est une couche d'authentification sur OAuth 2.0 permettant de vérifier l'identité utilisateur via tokens standardisés.\n\n**Différence OAuth** : OAuth = autorisation, OIDC = authentification + autorisation.\n\n**Tokens** : ID token (JWT avec sub, email, name), access token, refresh token.\n\n**Cas d'usage** : SSO, federation (Google, Azure AD), identity management. **Sécurité** : validation JWT, PKCE, nonce (replay protection)."
  },
  "en": {
    "short": "An authentication layer built on OAuth 2.0 enabling verification of user identity via standardized tokens",
    "full": "**OpenID Connect** (OIDC) is an authentication layer on top of OAuth 2.0 enabling verification of user identity via standardized tokens.\n\n**Difference from OAuth** : OAuth = authorization, OIDC = authentication + authorization.\n\n**Tokens** : ID token (JWT with sub, email, name), access token, refresh token.\n\n**Use cases** : SSO, federation (Google, Azure AD), identity management. **Security** : JWT validation, PKCE, nonce (replay protection)."
  },
  "en_status": "done"
}
