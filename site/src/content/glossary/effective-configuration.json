{
  "term": "Effective configuration",
  "slug": "effective-configuration",
  "category": "Pavois",
  "tags": [
    "Pavois",
    "audit",
    "configuration"
  ],
  "aliases": [
    "effective config",
    "configuration effective",
    "resolved configuration",
    "config effective"
  ],
  "related": [
    "drop-in",
    "include-directive",
    "cinc-inspec",
    "scap-oval"
  ],
  "fr": {
    "translation": "Configuration effective",
    "short": "L'état de configuration réellement appliqué par un service à l'exécution, après résolution des `Include` et des drop-ins, par opposition au texte d'un fichier.",
    "full": "La **configuration effective** est ce qu'un service applique réellement une fois démarré, et non ce qu'un fichier déclare.\n\nExemple : `sshd` charge `/etc/ssh/sshd_config`, ses directives `Include`, puis les drop-ins de `/etc/ssh/sshd_config.d/`. Seule la commande `sshd -T` révèle la valeur **résolue** de chaque option.\n\nC'est l'angle de **Pavois** : auditer l'état effectif (`sshd -T`, `sysctl -a`, `systemctl show`, `auditctl -l`) plutôt que de lire les fichiers, ce que font OVAL/oscap, au risque de faux positifs et faux négatifs dès qu'un `Include` ou un drop-in entre en jeu."
  },
  "en": {
    "short": "The configuration a service actually applies at runtime, once `Include`s and drop-ins are resolved, as opposed to the text of a single file.",
    "full": "**Effective configuration** is what a service truly applies once started, not what a file declares.\n\nFor example, `sshd` loads `/etc/ssh/sshd_config`, its `Include` directives, then the drop-ins under `/etc/ssh/sshd_config.d/`. Only `sshd -T` reveals the **resolved** value of each option.\n\nThis is **Pavois**'s angle: audit the effective state (`sshd -T`, `sysctl -a`, `systemctl show`, `auditctl -l`) instead of reading files, which is what OVAL/oscap do, risking false positives and false negatives as soon as an `Include` or a drop-in is involved."
  },
  "en_status": "done"
}