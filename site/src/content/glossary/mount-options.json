{
  "term": "Mount Options",
  "slug": "mount-options",
  "category": "securite",
  "tags": [
    "filesystem",
    "mounts",
    "hardening"
  ],
  "aliases": [
    "mount options",
    "noexec",
    "nosuid",
    "nodev"
  ],
  "related": [
    "suid-sgid",
    "permissions",
    "effective-configuration"
  ],
  "fr": {
    "translation": "Options de montage",
    "short": "Drapeaux appliqués par le noyau à un système de fichiers monté : **`noexec`** (pas d'exécution), **`nosuid`** (bits SUID ignorés), **`nodev`** (pas de nœuds de périphérique).",
    "full": "Sur `/tmp`, `/dev/shm`, `/var`..., ces options coupent les abus classiques : `noexec` retire le terrain de jeu d'un payload déposé, `nosuid` neutralise un setuid planté, `nodev` bloque un faux nœud disque.\n\nPavois lit le montage **effectif** (`/proc/self/mountinfo`, `findmnt`), pas `/etc/fstab` : une option jamais remontée n'est pas effective."
  },
  "en": {
    "short": "Kernel-enforced flags on a mounted filesystem: **`noexec`** (no execution), **`nosuid`** (SUID bits ignored), **`nodev`** (no device nodes).",
    "full": "On `/tmp`, `/dev/shm`, `/var`..., these cut classic abuses: `noexec` removes a dropped payload's launch pad, `nosuid` neutralises a planted setuid, `nodev` blocks a rogue disk node.\n\nPavois reads the **effective** mount (`/proc/self/mountinfo`, `findmnt`), not `/etc/fstab`: an option never remounted is not effective."
  },
  "en_status": "done"
}