{
  "term": "ASLR",
  "slug": "aslr",
  "category": "securite",
  "tags": [
    "kernel",
    "memory",
    "exploit-mitigation"
  ],
  "aliases": [
    "ASLR",
    "Address Space Layout Randomization",
    "randomize_va_space"
  ],
  "related": [
    "sysctl",
    "buffer-overflow",
    "hardening"
  ],
  "fr": {
    "translation": "Randomisation de l'espace d'adressage",
    "short": "Mitigation noyau qui **randomise les adresses mémoire** d'un processus à chaque exécution, rendant les exploits mémoire bien moins fiables.",
    "full": "L'**ASLR** place pile, tas et bibliothèques à des adresses imprévisibles, donc un attaquant ne peut plus prédire où sauter après un débordement. Réglé par `kernel.randomize_va_space` (valeur `2` = complet).\n\nC'est une [valeur sysctl](/glossary/#sysctl) à vérifier en **effectif** : un fichier qui la pose mais jamais appliqué ne protège pas."
  },
  "en": {
    "short": "A kernel mitigation that **randomizes a process's memory addresses** on each run, making memory-corruption exploits far less reliable.",
    "full": "**ASLR** places the stack, heap and libraries at unpredictable addresses, so an attacker cannot predict where to jump after an overflow. Controlled by `kernel.randomize_va_space` (value `2` = full).\n\nIt is a [sysctl value](/glossary/#sysctl) to verify **effectively**: a file that sets it but is never applied does not protect."
  },
  "en_status": "done"
}
