{
  "term": "GRUB",
  "slug": "grub",
  "category": "securite",
  "tags": [
    "bootloader",
    "boot",
    "linux"
  ],
  "aliases": [
    "GRUB",
    "GRUB2",
    "grub.cfg"
  ],
  "related": [
    "secure-boot",
    "kernel-module",
    "hardening"
  ],
  "fr": {
    "translation": "Chargeur d'amorçage GRUB",
    "short": "Le chargeur d'amorçage Linux le plus répandu : il s'exécute **avant le système** et choisit le noyau et ses paramètres de démarrage.",
    "full": "**GRUB** précède toute défense de l'OS : qui peut éditer une entrée au démarrage peut ajouter `init=/bin/bash` et obtenir un **root non authentifié**. Durcissement : mot de passe superutilisateur (`password_pbkdf2`) avec `--unrestricted`, `grub.cfg` en `0600`, options de durcissement sur la ligne de commande noyau.\n\nPavois lit le `grub.cfg` **généré** et `/proc/cmdline`, pas seulement `/etc/default/grub`."
  },
  "en": {
    "short": "The most common Linux bootloader: it runs **before the OS** and chooses the kernel and its boot parameters.",
    "full": "**GRUB** precedes every OS defense: whoever can edit a boot entry can append `init=/bin/bash` and get an **unauthenticated root**. Hardening: a superuser password (`password_pbkdf2`) with `--unrestricted`, `grub.cfg` at `0600`, hardening flags on the kernel command line.\n\nPavois reads the **generated** `grub.cfg` and `/proc/cmdline`, not just `/etc/default/grub`."
  },
  "en_status": "done"
}