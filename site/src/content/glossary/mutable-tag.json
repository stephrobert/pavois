{
  "term": "Mutable Tag",
  "slug": "mutable-tag",
  "category": "securite",
  "tags": [
    "security",
    "supply-chain",
    "versioning"
  ],
  "aliases": [],
  "related": [
    "supply chain attack",
    "pinning",
    "immutable infrastructure",
    "artifact"
  ],
  "fr": {
    "translation": "",
    "short": "Référence de version (comme v1 ou latest) qui peut être modifiée pour pointer vers un autre commit ou artefact, contrairement aux sha immuables",
    "full": "Un **mutable tag** (tag mutable) est une référence de version (v1, latest) qui peut être déplacée vers un commit/artefact différent, contrairement aux SHA immuables.\n\n**Risques sécurité** : attaquant peut rediriger tag mutable vers code malveillant, affectant tous les utilisateurs référençant ce tag.\n\n**Exemples** : GitHub Actions `@v4` (mutable) vs `@sha`, Docker `nginx:latest` vs `nginx@sha256:...`, npm `^1.0.0` vs lockfile.\n\n**Bonnes pratiques** : épingler par SHA/digest pour dépendances critiques, lockfiles, Dependabot pour mises à jour contrôlées."
  },
  "en": {
    "short": "A version reference (such as v1 or latest) that can be changed to point to a different commit or artifact, unlike immutable SHAs",
    "full": "A **mutable tag** is a version reference (v1, latest) that can be moved to a different commit/artifact, unlike immutable SHAs.\n\n**Security risks** : an attacker can redirect a mutable tag to malicious code, affecting every user referencing that tag.\n\n**Examples** : GitHub Actions `@v4` (mutable) vs `@sha`, Docker `nginx:latest` vs `nginx@sha256:...`, npm `^1.0.0` vs lockfile.\n\n**Best practices** : pin by SHA/digest for critical dependencies, lockfiles, Dependabot for controlled updates."
  },
  "en_status": "done"
}
