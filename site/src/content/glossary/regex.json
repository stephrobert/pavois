{
  "term": "Regex",
  "slug": "regex",
  "category": "fondamentaux",
  "tags": [
    "concepts",
    "programming",
    "patterns"
  ],
  "aliases": [
    "Expression régulières",
    "regex",
    "regexs"
  ],
  "related": [
    "pattern",
    "validation",
    "parsing",
    "search"
  ],
  "fr": {
    "translation": "Expression régulière",
    "short": "Motif textuel décrivant ensemble de chaînes correspondant à un pattern pour recherche et validation",
    "full": "Une **regex** (regular expression) est un motif textuel décrivant un ensemble de chaînes correspondant à un pattern.\n\n**Syntaxe** : `.` (tout), `*` (0+), `+` (1+), `?` (0-1), `^$` (début/fin), `[]` (classe), `()` (groupe), `|` (ou), `\\d\\w`.\n\n**Usages DevOps** : parsing logs, validation input, grep/sed/awk, routing, alerting rules.\n\n**Outils** : regex101.com, grep -E, sed, awk. **Attention** : regex complexes lentes, préférer parsers dédiés pour JSON/XML."
  },
  "en": {
    "short": "Textual pattern describing a set of strings matching a pattern for searching and validation",
    "full": "A **regex** (regular expression) is a textual pattern describing a set of strings that match a pattern.\n\n**Syntax** : `.` (any), `*` (0+), `+` (1+), `?` (0-1), `^$` (start/end), `[]` (class), `()` (group), `|` (or), `\\d\\w`.\n\n**DevOps uses** : log parsing, input validation, grep/sed/awk, routing, alerting rules.\n\n**Tools** : regex101.com, grep -E, sed, awk. **Caution** : complex regexes are slow, prefer dedicated parsers for JSON/XML."
  },
  "en_status": "done"
}
