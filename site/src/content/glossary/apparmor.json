{
  "term": "AppArmor",
  "slug": "apparmor",
  "category": "securite",
  "tags": [
    "mac",
    "kernel",
    "debian"
  ],
  "aliases": [
    "AppArmor"
  ],
  "related": [
    "mandatory-access-control",
    "selinux",
    "discretionary-access-control"
  ],
  "fr": {
    "translation": "AppArmor",
    "short": "Module de **contrôle d'accès obligatoire (MAC)** du noyau (famille Debian/Ubuntu) qui confine un programme via un **profil** basé sur les chemins de fichiers.",
    "full": "**AppArmor** attache à un exécutable un **profil** listant les fichiers, capacités et réseaux autorisés ; le reste est refusé par le noyau. Un profil est en mode `enforce` (bloque) ou `complain` (journalise seulement).\n\n`aa-status` donne l'état réel : un profil en `complain` ou non chargé ne protège pas. C'est l'équivalent Debian de [SELinux](/glossary/#selinux)."
  },
  "en": {
    "short": "The kernel **Mandatory Access Control (MAC)** module (Debian/Ubuntu family) that confines a program through a path-based **profile**.",
    "full": "**AppArmor** attaches a **profile** to an executable listing the allowed files, capabilities and networks; everything else is denied by the kernel. A profile is in `enforce` mode (blocks) or `complain` (logs only).\n\n`aa-status` gives the real state: a profile in `complain` or not loaded does not protect. It is the Debian equivalent of [SELinux](/glossary/#selinux)."
  },
  "en_status": "done"
}
