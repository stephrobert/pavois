{
  "term": "sysctl",
  "slug": "sysctl",
  "category": "securite",
  "tags": [
    "kernel",
    "network",
    "linux"
  ],
  "aliases": [
    "sysctl",
    "/proc/sys",
    "kernel parameters"
  ],
  "related": [
    "effective-configuration",
    "aslr",
    "hardening"
  ],
  "fr": {
    "translation": "Paramètres noyau",
    "short": "Interface du noyau Linux pour lire et écrire des **paramètres runtime** (réseau, mémoire, comportement) via `/proc/sys`, pilotée par la commande `sysctl`.",
    "full": "`sysctl` règle des **paramètres dynamiques du noyau** (ex. `net.ipv4.ip_forward`, `kernel.randomize_va_space`). La valeur **effective** est celle du noyau en cours (`sysctl -a`, `/proc/sys`), résolue depuis une pile de fichiers (`/usr/lib/sysctl.d/`, `/etc/sysctl.d/`, `/etc/sysctl.conf`).\n\nDurcissement : poser la valeur sûre **et** la rendre persistante dans `/etc/sysctl.d/`, car un `sysctl -w` runtime régresse au redémarrage."
  },
  "en": {
    "short": "The Linux kernel interface to read and write **runtime parameters** (network, memory, behaviour) via `/proc/sys`, driven by the `sysctl` command.",
    "full": "`sysctl` sets **dynamic kernel parameters** (e.g. `net.ipv4.ip_forward`, `kernel.randomize_va_space`). The **effective** value is the running kernel's (`sysctl -a`, `/proc/sys`), resolved from a stack of files (`/usr/lib/sysctl.d/`, `/etc/sysctl.d/`, `/etc/sysctl.conf`).\n\nHardening: set the safe value **and** persist it in `/etc/sysctl.d/`, because a runtime `sysctl -w` regresses on reboot."
  },
  "en_status": "done"
}