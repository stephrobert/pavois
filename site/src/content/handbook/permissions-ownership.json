{
  "id": "permissions-ownership",
  "section": "domains",
  "order": 6,
  "domain": "File permissions",
  "title": { "en": "File Permissions & Ownership", "fr": "Permissions et propriété des fichiers" },
  "summary": {
    "en": "Sensitive files like `/etc/shadow`, SSH keys and cron jobs are only as safe as their mode bits — tighten permissions, kill world-writable files and review SUID/SGID so a low-privilege account can't read secrets or escalate.",
    "fr": "Des fichiers sensibles comme `/etc/shadow`, les clés SSH et les tâches cron ne valent que ce que valent leurs bits de permission — resserrer les droits, éliminer les fichiers modifiables par tous et passer en revue les SUID/SGID pour qu'un compte peu privilégié ne puisse ni lire de secrets ni s'élever."
  },
  "cite": [
    {
      "url": "https://blog.stephane-robert.info/docs/securiser/durcissement/acl/",
      "label": "Affiner avec les ACL"
    }
  ],
  "body": {
    "en": "## The threat\n\nDiscretionary access control is the **last line of defense** when an attacker already has an account on the box. A loose permission turns a foothold into a breach: a world-readable `/etc/shadow` hands over every password hash for offline cracking; a group-writable `/etc/passwd`, `/etc/sudoers` or `/etc/crontab` lets an unprivileged user **rewrite identity, sudo policy or a root cron job** and escalate at will. **World-writable files** (`-rw-rw-rw-`) in shared paths like `/tmp` are a classic vector for symlink and config-injection attacks. **SUID/SGID** binaries run with the owner's privileges regardless of who launches them — a single misplaced setuid root binary is a direct path to root. And SSH host keys or private keys that are readable by `other` undermine the whole trust chain.\n\n## Why harden it\n\nBecause permissions are **cheap to get wrong and catastrophic when wrong**. There is no exploit to write: the attacker simply *reads* the file or *runs* the binary the kernel already lets them touch. Hardening this domain means that even after an account is compromised, the secrets stay unreadable, the privilege-policy files stay untouchable, and there are no surprise paths to root — **shrinking the blast radius** of every other failure on the system.\n\n## Defense principles applied\n\n- **Least privilege** — secrets get the minimum mode: `/etc/shadow` and `/etc/gshadow` unreadable to group/other, `/etc/sudoers` root-only, audit logs `0640` or stricter, SSH private material protected.\n- **Attack-surface reduction** — no **unauthorized world-writable** files; no stray **SUID/SGID** bits where they aren't needed; the `setuid`/`setgid`/`sticky` bits are reviewed on every sensitive file.\n- **Integrity over confidentiality where it matters** — `/etc/passwd` must stay world-readable to function, so the rule is that it is **not writable** by group/other; the policy and credential files (`sudoers`, `crontab`, `shadow`) get both.\n- **Accountability** — log files under `/var/log` (auth, syslog, audit) are locked down so an attacker can't read or tamper with the trail.\n- **Fine-grained control with ACLs** — when plain owner/group/other modes are too coarse, **POSIX ACLs** (`setfacl`/`getfacl`) grant a specific user or group exactly the access needed, without widening the mode for everyone.\n\n## What Pavois audits\n\nPavois's **File permissions** domain ships dozens of InSpec rules that resolve the *actual* mode and ownership on the target (via CINC's `file` resource), not what a config-management template claims. It checks the credential and identity files — `/etc/shadow` and `/etc/gshadow` carry **no** read/write/execute for owner, group or other, `/etc/passwd` and `/etc/group` are **not writable** by group/other, and their `.bak` backups too. It locks down privilege policy (`/etc/sudoers`, `/etc/sudoers.d/`), the full **cron** surface (`/etc/crontab`, `/etc/cron.{hourly,daily,weekly,monthly,d}`, `cron.allow`/`cron.deny`, `at.allow`/`at.deny`), **SSH** material (`/etc/ssh`, host keys, `*.pub`, the `sshd_config.d/` drop-ins), bootloader files (GRUB, EFI), and the **log** trail (`/var/log` and `audit`, `auth`, `syslog`, `messages`, `lastlog`...). Every rule asserts the **`setuid`/`setgid`/`sticky` bits** are off where they must be, and a dedicated **world-writable** canary verifies shared paths like `/tmp` are not writable by `other`."
,
    "fr": "## La menace\n\nLe contrôle d'accès discrétionnaire est la **dernière ligne de défense** quand un attaquant a déjà un compte sur la machine. Une permission relâchée transforme un pied dans la place en compromission : un `/etc/shadow` lisible par tous livre tous les hachages de mots de passe pour un cassage hors-ligne ; un `/etc/passwd`, `/etc/sudoers` ou `/etc/crontab` modifiable par le groupe permet à un utilisateur non privilégié de **réécrire les identités, la politique sudo ou une tâche cron root** et de s'élever à volonté. Les **fichiers modifiables par tous** (`-rw-rw-rw-`) dans des chemins partagés comme `/tmp` sont un vecteur classique d'attaques par lien symbolique et par injection de configuration. Les binaires **SUID/SGID** s'exécutent avec les privilèges du propriétaire quel que soit l'appelant — un seul binaire setuid root mal placé est un chemin direct vers root. Et des clés d'hôte ou clés privées SSH lisibles par `other` minent toute la chaîne de confiance.\n\n## Pourquoi le durcir\n\nParce que les permissions sont **faciles à rater et catastrophiques quand elles le sont**. Aucun exploit à écrire : l'attaquant *lit* simplement le fichier ou *exécute* le binaire que le noyau l'autorise déjà à toucher. Durcir ce domaine garantit que, même après la compromission d'un compte, les secrets restent illisibles, les fichiers de politique de privilèges restent intouchables et qu'il n'existe aucun chemin surprise vers root — **réduisant le rayon d'impact** de toute autre faille du système.\n\n## Principes de défense appliqués\n\n- **Moindre privilège** — les secrets reçoivent le mode minimal : `/etc/shadow` et `/etc/gshadow` illisibles par le groupe/les autres, `/etc/sudoers` réservé à root, journaux d'audit en `0640` ou plus strict, matériel SSH privé protégé.\n- **Réduction de la surface d'attaque** — aucun fichier **modifiable par tous** non autorisé ; aucun bit **SUID/SGID** parasite là où il est inutile ; les bits `setuid`/`setgid`/`sticky` sont revus sur chaque fichier sensible.\n- **Intégrité plutôt que confidentialité quand il le faut** — `/etc/passwd` doit rester lisible par tous pour fonctionner ; la règle est donc qu'il soit **non modifiable** par le groupe/les autres ; les fichiers de politique et d'identifiants (`sudoers`, `crontab`, `shadow`) reçoivent les deux.\n- **Traçabilité** — les fichiers journaux sous `/var/log` (auth, syslog, audit) sont verrouillés pour qu'un attaquant ne puisse ni lire ni altérer les traces.\n- **Contrôle fin avec les ACL** — quand les modes simples propriétaire/groupe/autres sont trop grossiers, les **ACL POSIX** (`setfacl`/`getfacl`) accordent à un utilisateur ou un groupe précis exactement l'accès nécessaire, sans élargir le mode pour tout le monde.\n\n## Ce que Pavois audite\n\nLe domaine **Permissions des fichiers** de Pavois embarque des dizaines de règles InSpec qui résolvent le mode et la propriété *réels* sur la cible (via la ressource `file` de CINC), et non ce qu'un modèle de gestion de configuration prétend. Il vérifie les fichiers d'identifiants et d'identité — `/etc/shadow` et `/etc/gshadow` ne portent **aucun** droit de lecture/écriture/exécution pour le propriétaire, le groupe ou les autres, `/etc/passwd` et `/etc/group` sont **non modifiables** par le groupe/les autres, ainsi que leurs sauvegardes `.bak`. Il verrouille la politique de privilèges (`/etc/sudoers`, `/etc/sudoers.d/`), toute la surface **cron** (`/etc/crontab`, `/etc/cron.{hourly,daily,weekly,monthly,d}`, `cron.allow`/`cron.deny`, `at.allow`/`at.deny`), le matériel **SSH** (`/etc/ssh`, clés d'hôte, `*.pub`, les drop-ins `sshd_config.d/`), les fichiers du chargeur d'amorçage (GRUB, EFI) et les **traces** de journalisation (`/var/log` et `audit`, `auth`, `syslog`, `messages`, `lastlog`...). Chaque règle vérifie que les **bits `setuid`/`setgid`/`sticky`** sont absents là où ils doivent l'être, et une sentinelle **modifiable par tous** dédiée confirme que les chemins partagés comme `/tmp` ne sont pas modifiables par `other`."
  }
}
