# Embedded norm catalogue (docs/reference/norms.yml). Populated by `mise run embed:reference`
# before a release build. A separate directory from content/, which holds the per-OS references:
# norms.yml is not a system, and it has no business appearing in the list of supported systems.
