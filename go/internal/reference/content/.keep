# Embedded hardening reference. Populated by `mise run embed:reference` (copied from
# ../../../../docs/reference/pavois-content) before a release build, so `harden plan`, `rules`,
# `norms` and `oscal` work from a downloaded binary. Git-ignored except this .keep, so the embed
# compiles in a dev build (empty -> the commands fall back to the on-disk reference).
