# Embedded apply-time data: audit.rules and kernel-build/<os>.sh. Populated by
# `mise run embed:reference` before a release build.
#
# These were read with os.ReadFile under findRoot() AND THE ERROR DISCARDED, so a downloaded binary
# applied a hardening plan with an EMPTY audit ruleset and an EMPTY kernel recipe, silently, and
# reported success. Worse than #286, which at least failed loudly.
