# Embedded rule corpus. Populated by `mise run render` (copied from ../../../profiles) before a
# release build so the shipped binary is self-contained. Git-ignored except this .keep so the
# embed compiles in a dev build (empty corpus -> the binary falls back to the on-disk profiles/).
