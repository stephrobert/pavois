{
  "id": "sysctl-net-ipv4-icmp_echo_ignore_broadcasts",
  "title": {
    "en": "Enable Kernel Parameter to Ignore ICMP Broadcast Echo Requests on IPv4 Interfaces",
    "fr": "Activer le paramètre noyau pour ignorer les requêtes echo ICMP en broadcast sur les interfaces IPv4"
  },
  "summary": {
    "en": "Makes the kernel ignore ICMP echo requests sent to broadcast/multicast addresses by setting `net.ipv4.icmp_echo_ignore_broadcasts` to **1**.",
    "fr": "Fait ignorer au noyau les requêtes echo ICMP envoyées vers des adresses broadcast/multicast en positionnant `net.ipv4.icmp_echo_ignore_broadcasts` à **1**."
  },
  "rationale": {
    "en": "Responding to broadcast (ICMP) echoes facilitates network mapping and provides a vector for **amplification attacks** (e.g. Smurf), where spoofed broadcast pings turn the subnet into a traffic multiplier against a victim. Ignoring ICMP echo requests sent to broadcast or multicast addresses makes the system harder to enumerate and removes it from such amplification.",
    "fr": "Répondre aux echos ICMP en broadcast facilite la cartographie du réseau et offre un vecteur d'**attaques par amplification** (ex. Smurf), où des pings broadcast usurpés transforment le sous-réseau en multiplicateur de trafic contre une victime. Ignorer les requêtes echo ICMP envoyées en broadcast ou multicast rend le système plus difficile à énumérer et le retire de ces amplifications."
  },
  "check_note": {
    "en": "Pavois reads the **live** value via `kernel_parameter('net.ipv4.icmp_echo_ignore_broadcasts')`, reflecting the running kernel after `sysctl.conf`, `/etc/sysctl.d/` drop-ins and runtime overrides are merged. A scan of `/etc/sysctl.conf` alone could report a value that a later drop-in or `sysctl -w` has overridden.",
    "fr": "Pavois lit la valeur **effective** via `kernel_parameter('net.ipv4.icmp_echo_ignore_broadcasts')`, qui reflète le noyau en cours après fusion de `sysctl.conf`, des drop-ins de `/etc/sysctl.d/` et des surcharges runtime. Un examen du seul `/etc/sysctl.conf` pourrait rapporter une valeur qu'un drop-in ultérieur ou un `sysctl -w` a écrasée."
  },
  "verify": {
    "en": "Run `sysctl net.ipv4.icmp_echo_ignore_broadcasts` and expect `net.ipv4.icmp_echo_ignore_broadcasts = 1`.",
    "fr": "Exécuter `sysctl net.ipv4.icmp_echo_ignore_broadcasts` et attendre `net.ipv4.icmp_echo_ignore_broadcasts = 1`."
  },
  "logs": {
    "en": "Confirm with `sysctl net.ipv4.icmp_echo_ignore_broadcasts`. To prove the behavior, send `ping -b <broadcast-addr>` from another host and verify the target does not reply; observe traffic with `tcpdump -ni any icmp`.",
    "fr": "Confirmer avec `sysctl net.ipv4.icmp_echo_ignore_broadcasts`. Pour prouver le comportement, envoyer `ping -b <adresse-broadcast>` depuis un autre hôte et vérifier que la cible ne répond pas ; observer le trafic avec `tcpdump -ni any icmp`."
  },
  "remediation_note": {
    "en": "Pavois's harden plan sets `net.ipv4.icmp_echo_ignore_broadcasts = 1` via the **sysctl** resource, persisting it in a Pavois-owned drop-in and applying it live (no reboot). Run it with `pavois harden apply`.",
    "fr": "Le plan de durcissement de Pavois positionne `net.ipv4.icmp_echo_ignore_broadcasts = 1` via la ressource **sysctl**, en le persistant dans un drop-in géré par Pavois et en l'appliquant à chaud (sans redémarrage). À exécuter avec `pavois harden apply`."
  },
  "impact": {
    "en": "No practical downside on servers or workstations, only **directed-broadcast pings** are dropped, while normal unicast ping still works. Precaution: rare network-diagnostic tooling that relies on broadcast ping sweeps to discover hosts will no longer see this machine, which is the intended hardening effect.",
    "fr": "Aucun inconvénient pratique sur des serveurs ou postes de travail, seuls les **pings broadcast dirigés** sont abandonnés, le ping unicast normal continue de fonctionner. Précaution : de rares outils de diagnostic réseau qui s'appuient sur des balayages ping broadcast pour découvrir des hôtes ne verront plus cette machine, ce qui est précisément l'effet de durcissement recherché."
  },
  "datePublished": "2026-06-28",
  "dateModified": "2026-07-14"
}
