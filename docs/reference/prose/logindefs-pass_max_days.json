{
  "id": "logindefs-pass_max_days",
  "title": {
    "en": "Set Password Maximum Age",
    "fr": "Définir l'âge maximal des mots de passe"
  },
  "summary": {
    "en": "Ensures `PASS_MAX_DAYS` in `/etc/login.defs` is **365 or fewer**, forcing periodic password changes for new accounts.",
    "fr": "Garantit que `PASS_MAX_DAYS` dans `/etc/login.defs` vaut **365 ou moins**, imposant un changement périodique des mots de passe pour les nouveaux comptes."
  },
  "rationale": {
    "en": "Any password can eventually be cracked, so passwords must be changed periodically. If the system never forces a password change, a compromised credential stays valid indefinitely. Setting `PASS_MAX_DAYS` to **365 or fewer** bounds how long a leaked or guessed password remains usable. (Setting it too low is counterproductive, encouraging users to write passwords down.)",
    "fr": "Tout mot de passe finit par être cassé, ils doivent donc être changés périodiquement. Si le système ne force jamais le changement, un identifiant compromis reste valide indéfiniment. Définir `PASS_MAX_DAYS` à **365 ou moins** borne la durée pendant laquelle un mot de passe fuité ou deviné reste utilisable. (Le fixer trop bas est contre-productif et incite à noter les mots de passe.)"
  },
  "check_note": {
    "en": "Pavois uses the InSpec `login_defs` resource to read the **effective resolved value** of `PASS_MAX_DAYS`, parsing `/etc/login.defs` the way `useradd`/`chage` interpret it (last uncommented setting wins). This is the default applied to newly created accounts; per-user values in `/etc/shadow` may differ and are governed separately.",
    "fr": "Pavois utilise la ressource InSpec `login_defs` pour lire la **valeur effective résolue** de `PASS_MAX_DAYS`, en analysant `/etc/login.defs` comme le font `useradd`/`chage` (la dernière valeur non commentée l'emporte). C'est le défaut appliqué aux comptes nouvellement créés ; les valeurs par utilisateur dans `/etc/shadow` peuvent différer et sont gérées séparément."
  },
  "verify": {
    "en": "- `grep -i '^PASS_MAX_DAYS' /etc/login.defs` should print `PASS_MAX_DAYS 365` (or less).\n- Per existing account: `chage -l <user>` shows `Maximum number of days between password change`.",
    "fr": "- `grep -i '^PASS_MAX_DAYS' /etc/login.defs` doit afficher `PASS_MAX_DAYS 365` (ou moins).\n- Par compte existant : `chage -l <user>` montre `Maximum number of days between password change`."
  },
  "logs": {
    "en": "- `grep -i PASS_MAX_DAYS /etc/login.defs` shows the default.\n- `chage -l <user>` shows each account's effective maximum age.\n- `/var/log/auth.log` (Debian/Ubuntu) or `/var/log/secure` (RHEL) record password-expiry and change events via PAM.",
    "fr": "- `grep -i PASS_MAX_DAYS /etc/login.defs` montre le défaut.\n- `chage -l <user>` montre l'âge maximal effectif de chaque compte.\n- `/var/log/auth.log` (Debian/Ubuntu) ou `/var/log/secure` (RHEL) enregistrent les expirations et changements de mot de passe via PAM."
  },
  "remediation_note": {
    "en": "No automated harden plan is defined for this rule, so it must be applied **manually**: set `PASS_MAX_DAYS 365` (or less) in `/etc/login.defs`. This only governs **newly created** accounts; apply `chage --maxdays 365 <user>` to bring existing accounts into line.",
    "fr": "Aucun plan de durcissement automatisé n'est défini pour cette règle, elle doit donc être appliquée **manuellement** : définissez `PASS_MAX_DAYS 365` (ou moins) dans `/etc/login.defs`. Cela ne régit que les comptes **nouvellement créés** ; utilisez `chage --maxdays 365 <user>` pour aligner les comptes existants."
  },
  "impact": {
    "en": "**Caution:** existing accounts are not changed by editing `login.defs` alone, and applying `chage` to all users can suddenly expire passwords if `lastchange` predates the new window, service/automation accounts can lock out. Exclude or set `PASS_MAX_DAYS -1` for non-interactive system accounts, and prefer key-based auth for them. Modern guidance (NIST SP 800-63B) discourages forced rotation absent evidence of compromise; align with your policy.",
    "fr": "**Attention :** modifier `login.defs` seul ne change pas les comptes existants, et appliquer `chage` à tous les utilisateurs peut expirer brutalement les mots de passe si `lastchange` précède la nouvelle fenêtre, les comptes de service/automatisation peuvent se verrouiller. Excluez ou fixez `PASS_MAX_DAYS -1` pour les comptes système non interactifs, et privilégiez l'authentification par clé pour eux. Les recommandations récentes (NIST SP 800-63B) déconseillent la rotation forcée sans preuve de compromission ; alignez-vous sur votre politique."
  },
  "datePublished": "2026-06-28",
  "dateModified": "2026-07-14"
}
